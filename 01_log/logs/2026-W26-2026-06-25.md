log 25.06.2026 23:54 Claude Code (Windows / JUSTMAIN) — **/push: 0 Repos (alle sauber) — nur dieser Log-Eintrag; nachgetragen wird die heutige Server-Portabilität.** **[[itintech-firma]] / Itin-TechSolutions** `b5fcc5f` (`feat:`) wurde schon im vorigen Task gepusht, hier der Log dazu: **portables, self-contained MC-Server-Paket für Windows UND macOS** unter `20_Konzepte/AIPlayGames/Minecraft/server/`. **Warum:** Justin schaltet den Gaming-PC ab → der **Mac mini soll Server + Chatlog + LLM-Chat-API übernehmen**. Struktur: `common/` Node-Tools (`chatlog.js` + `chat-api.js`, **plattformübergreifend** — der bisher PowerShell-only Chat-Watcher ist jetzt Node, läuft auf dem Mac genauso, behebt nebenbei den UTF-8/Umlaut-Bug), `config/` Templates **ohne Secrets**, `windows/start.ps1`+`.bat` und `mac/start.sh` (beide **idempotent**: Jar 1.21.11 **SHA1-gepinnt** laden, `server.properties` mit Zufalls-RCON-PW + `apikey.txt` einmalig erzeugen, Altinstanzen stoppen, Server+Watcher+API starten). `.gitignore` hält Jar/Welt/Logs/Secrets draussen, **`.gitattributes` erzwingt LF für `start.sh`** (sonst `^M`-Crash auf macOS). Portable API gegen den Live-Server getestet (POST/GET/RCON ok). **Vorgelagerte Runtime-Arbeit (bewusst NICHT im Repo, lokal auf JUSTMAIN):** die **Chat-API** gebaut, über die **LLMs den Server-Chat lesen/schreiben** (HTTP + `X-API-Key`, nur Tailscale, `tellraw` wird zusätzlich in `chat.txt` gespiegelt damit mehrere LLMs sich gegenseitig sehen); den **Server am Vorabend von 26.2 → 1.21.11 gepinnt** (Mineflayer-Limit, bestätigt Doc 04), diverse neue Welten generiert, `world_GOOD_backup` + Seed gesichert. **Secrets sauber:** `server.properties` (enthält `rcon.password`) und `apikey.txt` per `.gitignore` raus, nur Templates committet; `git diff --cached` gegengeprüft. **Übersprungen** (alle ahead=0/behind=0): agentic-ai-server, JorllyJustinTransfer, PrivateBackup, rack-viewer. Alles sauber gepusht — Justin kann schlafen; offen bleibt nur die **Verifikation auf dem Mac selbst** (Java 21+/node prüfen, `start.sh` laufen lassen). → [[itintech-firma]] [[mindcraft-ben-setup]] [[mac-mini-api-keys]]

log 25.06.2026 23:18 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **Ben redet ~8× schneller — echter, gemessener Latenz-Fix (committet).** **[[itintech-firma]] / Itin-TechSolutions** `4354dc0` (`perf:`). **Ursache gefunden:** Ich hatte Ben vorher mit 87 KB Spielwissen aus `learn/` gefüttert (`knowledge.md`, **30 600 Tokens**), aber der Block stand im conversing-Prompt **ganz hinten**, hinter dem volatilen `$STATS`/`$INVENTORY`/`$EXAMPLES` → vLLM-**Prefix-Cache nie warm** → **jede** Antwort ~8 s Prefill, bevor Ben ein Wort sagt (Justin: „er braucht zu lange zum reden"). **Gemessen** (max_tokens, thinking aus): minimal 1,7 s · 87 KB kalt **8,0 s** · warm **1,07 s**. **Fix:** (1) `prompter.js` — neuer `$KNOWLEDGE`-Loader, vom volatilen `$LESSONS` getrennt; (2) `_default.json` — `$KNOWLEDGE` an den **Prompt-Anfang** (stabiler Prefix) → ab 2. Nachricht ~1 s statt ~8 s, mit wechselndem Stats-Tail bewiesen (Runde 2/3 = 1,0 s); (3) `warmup.js` + `start-ben.sh` — heizt den Cache **vor** `main.js` → auch die **1. Nachricht** warm (gemessen 1,15 s). Holistisch: **alle** Antworten ~1 s statt ~8 s. (4) `reflexion.js` — `lessons.md`-**Cap** (letzte 8 Reflexionsblöcke, Rest → `lessons-archive.md`), Logik isoliert getestet (8 behalten/2 archiviert/Kopf intakt). **Push:** Remote war **voraus 2** → `git pull --rebase` (fremde `reflexion.service` kurz gestasht, kein Force, konfliktfrei). **Bewusst NICHT mitcommittet:** Justins fremde `trainer-bot{,2,3}.js` + `debug-connect.mjs` (seine Trainings-Experimente) und eine fremde `reflexion.service`-Änderung (zeigt jetzt auf Repo-Pfad statt `~/mindcraft` → würde Reflexion in die falsche `bots/Ben/` schreiben, **Justin gewarnt**). `knowledge.md` ist gitignored (`bots/**`), regenerierbar aus committetem `learn/`. **Neue MC-Welt:** Ben hat den Welt-Wechsel **selbst** übernommen (Memory rollte weiter, „neue map → neu anfangen"), **kein** Reset nötig. **Übersprungen:** alle anderen Repos sauber. → [[itintech-firma]] [[mindcraft-ben-setup]] [[rechner-gb10]]

log 25.06.2026 23:00 Claude Code (Windows / JUSTMAIN) — **/push: 1 Repo gepusht — Itin-TechSolutions.** **Itin-TechSolutions** `7d4261c` (`docs:`) — committet den im 20:36-Lauf bewusst zurückgehaltenen Vault `20_Projekte/20_Konzepte/ChatgptMemory/` (116 Dateien): ChatGPT-Memory-Konzept (Person/Haltung/Firma/Finanzen/Projekte/Hardware/PiCrawler) + kompletter **NexusInput**-Snapshot (00_Log-Historie, 10_Personen, 20_Projekte-Konzepte, 30_Agenten Ben/Luna, 50_Systeme, 60_Referenz, 70_Arbeit, 09_roboter/picrawler, nexus/utils-Parser). **Secrets diesmal sauber raus:** der Filename-Regex übersah die Tokens (heissen `BraveAPI.txt`/`Telegram*.txt`, nicht `*key*`) → manuell erkannt, die **3 Token-Files** (Brave-Search-Key + 2 Telegram-Bot-Tokens unter `04_agenten/APIKeys/`) per neuer `.gitignore`-Regel ausgeschlossen, nur deren `README.md` mit drin; per `git diff --cached` verifiziert dass **kein** Secret gestaged ist. Repo-Visibility vorab als **privat** bestätigt (anonyme GitHub-API → 404), da Finanz-/Kunden-/Versicherungsdaten drin. **Übersprungen** (sauber, ahead=0/behind=0): agentic-ai-server, JorllyJustinTransfer, PrivateBackup, rack-viewer; **nexus** nur dieser Log-Eintrag. → [[itintech-firma]] [[claudesync]]

log 25.06.2026 22:54 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 1 Repo, 2 Commits — Minecraft-Doku auf Leistung getrimmt.** Folgt direkt auf den 22:34-Lauf (der die Struktur eincheckte); jetzt der Inhalts-Politur-Durchgang. **[[itintech-firma]] / Itin-TechSolutions** `5f6b551` (`docs:`) — Doku konsistent + **Performance-Roadmap**. Konkret: **gpt-oss-120b/qwen3-moe-Leichen raus** (docs `02`+`05` nannten ein falsches Modell, `02` widersprach sich sogar selbst `qwen3-moe` vs `gpt-oss-120b`) → korrigiert auf das **reale `nvidia/Qwen3.6-35B-A3B-NVFP4` + `api:vllm`** (verifiziert gegen `config/andy.json` + laufenden `vllm-qwen36`-Container). **`config/README.md`** neu: `config/` war **byte-identische Kopie** von `code/Ben/` → als Snapshot markiert (Anti-Drift, code/Ben kanonisch). **`upgrades/ROADMAP.md`** neu: priorisiert nach Impact÷Aufwand — **P0** vLLM-`--max-model-len` 262k→16–32k (gibt zweistellige GiB Unified Memory frei, gegen den Swap-Druck von heute Abend) + **lessons.md-Cap** (`reflexion.js` dedupt zwar, hat aber **keinen Grössen-Cap** → jede Zeile geht in jeden Prompt); **P1** Pathfinder→Mining→Restart-Schleifen; **P2** Skill-Playbooks; **P3** systemd/Server-Umzug. **doc `08` korrigiert:** Increment 2 (Reflexions-Motor `reflexion.js`+Timer, lief real 20:14) war als „nächster Schritt" gelabelt → auf **gebaut** gesetzt. `README-Alt` als **VERALTET** gebannert; README-Struktur an Realität (learn/, logs/) angeglichen. Plus **`learn/`**: Bens selbst-generiertes Spielwissen (blocks/items/crafting/mobs/biomes) eingecheckt. — Zweiter Commit `f135a69` (`chore:`): **eigenen Fehler korrigiert** — mein erstes `git add -A` zog den vom 22:34-Lauf **bewusst ausgeschlossenen** `logs/chat/`-Runtime-Chatlog wieder rein; per `git rm --cached` + neuer `Minecraft/.gitignore` dauerhaft draussen. **Nebenbei (kein Commit):** vorher Open-WebUI-Container + `tictactoe` gestoppt → GPU **94%→1%**, Desktop entlastet; `vllm-qwen36` + **Hermes-Telegram-Gateway (Ben)** bewusst weiterlaufen lassen. **nexus** (dieser Commit, `docs:`) — nur dieser Log-Eintrag, gezielt gestaged. **Übersprungen** (sauber, ahead=0/behind=0): Cirqit, JorllyJustinTransfer, JustUpdate, PrivateBackup, itintechsolutionswebsite, rack-viewer. → [[itintech-firma]] [[mindcraft-ben-setup]] [[open-webui-gx10]] [[rechner-gb10]]

log 25.06.2026 22:34 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 2 Repos gepusht.** **[[itintech-firma]] / Itin-TechSolutions** `90dc30f` (`docs:`) — **Minecraft-Konzept restrukturiert** (passt zum Windows-Eintrag von 20:36, der die 7 flachen Doku-Dateien angelegt hatte): die flachen `01–06`-Dateien sind jetzt nach `docs/` verschoben, README als Index überarbeitet. Neu als Quelle eingecheckt: der lokale **Mindcraft-Fork „Ben"** unter `code/Ben/` (226 Dateien, 3,5 MB) — **secret-geprüft sauber**: kein `node_modules`, kein verschachteltes `.git`, **kein echtes `keys.json`** (nur das leere `keys.example.json`-Template + der `keys.js`-Loader, keine hardcodierten Tokens). Laufzeit-Konfig + LLM-Profile unter `config/`. **Bewusst NICHT committet:** `Minecraft/logs/` (einzelner Runtime-Chatlog `chat-2026-06-25.txt`) — Laufzeit-Artefakt, gehört nicht ins Konzept-Repo. **nexus** (dieser Commit, `docs:`) — war **behind 1** → `git pull --rebase` (fast-forward, konfliktfrei: frontmatter/`master.md`/`tree.md`/service-ports-Updates), dann **gezielt** gestaged (kein `add -A`): neues `03_projekte/justreise-persistence.md` (Geschwister von `justreise.md`), `06_systeme/tailscale-netzwerk.md` + `acino-lohn-vorbereitung.md` (beide secret-geprüft sauber), sowie Python-Packaging `pyproject.toml` + `nexus/__init__.py` für die Luna-Parser-Module. **Übersprungen** (sauber, ahead=0/behind=0): Cirqit, JorllyJustinTransfer, JustUpdate, PrivateBackup, itintechsolutionswebsite, rack-viewer. → [[itintech-firma]] [[justreise]] [[rechner-gb10]]

log 25.06.2026 20:36 Claude Code (Windows / JUSTMAIN) — **/push: 1 Repo gepusht — Itin-TechSolutions.** **Itin-TechSolutions** `7a4c1e8` (`docs:`) — neue Konzept-Doku `20_Konzepte/AIPlayGames/Minecraft/` (7 Dateien): „lokale KI spielt Minecraft". Enthält: lokalen MC-**Testserver auf JUSTMAIN** (26.2, Java 25 nötig, `online-mode=false`, Firewall nur Tailscale `100.94.200.83:25565`, verifiziert per Server-List-Ping), Software-Stack (**Mineflayer+Mindcraft** als Körper/Brücke, **vLLM+Qwen3-MoE** als Hirn — Justin nutzt vLLM, nicht Ollama), GX10-Setup, **Versions-Kompatibilität** (Mineflayer kann 26.2 evtl. noch nicht → Server pinnen) und die Architektur-Entscheidung **Hermes-Vision+xdotool vs. Mineflayer-Protokoll** (Ergebnis: Mineflayer-Körper, Hermes bleibt Hirn). **Bewusst NICHT committet:** untracked `20_Konzepte/ChatgptMemory/` — grosser persönlicher Vault aus separatem Nacht-Lauf mit **live Secrets** (`04_agenten/APIKeys/*.txt`: Brave/Telegram-Tokens, evtl. `Luna/config.yaml`); gehört nicht in dieses „dann push", Keys müssen erst raus. **Übersprungen** (sauber, ahead=0/behind=0): agentic-ai-server, JorllyJustinTransfer, PrivateBackup, rack-viewer; **nexus** nur dieser Log-Eintrag. → [[itintech-firma]]
