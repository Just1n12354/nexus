log 26.06.2026 22:46 Claude Code (Windows / JUSTMAIN) — **/push: 1 Repo gepusht — Doom-RL-Agent + Minecraft-Research abgelegt.** **[[itintech-firma]] / Itin-TechSolutions** zwei Commits: `2941713` (`feat:`) = neues **Doom-RL-Projekt** unter `AIPlayGames/Doom/` (VizDoom-Setup: Q-Learning-Trainer `q_learn.py`, LLM-gesteuerter Agent `agent_llm.py`, Watch-Skripte, plus Trainingsdaten/Lessons/Q-Table); `bde1bf7` (`docs:`) = `AIPlayGames/Minecraft/research/` mit Link-Sammlung (Internet `Links.txt` + YouTube `Videos.txt`). Auf Secrets geprüft — der einzige Grep-Treffer war `showsecrets`/`am_secretwallcolor` in `_vizdoom.ini` (Doom-Engine-Settings, **keine** Credentials); `__pycache__`/`.pyc` korrekt via `.gitignore` draussen. **Push-Notiz:** erster `push` als non-fast-forward abgelehnt (Remote war während des Laufs durch Parallel-Lauf auf `3ce785f` „Berta-Auftrags-System" vorgerückt); ein paralleler Prozess hat dann sauber gemerged+gepusht (Merge-Commit `149c8e5`) — **kein Force**, beide meine Commits liegen verifiziert in `origin/main`, Working Tree clean, kein offener Merge. **nexus** clean, nur dieser Log-Eintrag. **Übersprungen** (sauber, ahead=0/behind=0): agentic-ai-server, JorllyJustinTransfer, PrivateBackup, rack-viewer. → [[itintech-firma]] [[mindcraft-ben-setup]]

log 26.06.2026 21:15 Claude Code (Windows / JUSTMAIN) — **/push: 1 Repo gepusht — Minecraft-Projekt auf die Berta-Linie umgebaut.** **[[itintech-firma]] / Itin-TechSolutions** `c8d0a02` (`refactor:`). Den `AIPlayGames/Minecraft/`-Ordner total umstrukturiert, weil die eigentliche Baustelle nicht fehlende Features waren, sondern **Verzettelung**: zwei Bot-Generationen (Ben/BotV1, Berta/BotV2) **plus zwei parallele BotV2-Varianten** (`claudecode`, `hermes`) — viel gebaut, nichts entschieden, nichts gemessen. **(1) Berta kanonisch:** `BotV2/claudecode` → **`bot/`** als einzige Weiterbau-Linie; Ben (Mindcraft-Fork) und die verworfene `hermes`-Variante nach **`_legacy/`** archiviert (nicht gelöscht — Git erkennt 323 Renames, History intakt); `server/` + Konzeptdocs `docs/` auf Top-Ebene gehoben. **(2) Die fehlende Mess-Schicht gebaut** (genau das, was die Architektur brauchte, statt mehr „Hirn"): **`bot/evals/`** = Benchmark-Arena mit 5 reproduzierbaren Tasks (Holz/Werkbank/Holzspitzhacke/Cobble/Zombie-Flucht, deterministisch per `/fill`·`/give`·`/summon`), Runner mit **skill-Modus** (ohne LLM, reproduzierbar) + **planner-Modus** (ganzer Stack), harte Erfolgsprüfung am echten WorldState, JSON-Reports als Vorher/Nachher-Beleg; **`src/learning/failures.js`** = Failure-Taxonomie → klassifizierte `failures.jsonl` statt Prosa; **`src/reflexes/`** = Reflexe als eigenes Modul; Docs **`EVALS.md`** (Doktrin „erst messen, dann bauen") + **`FAILURE_MODES.md`**, Top-`readme.md` neu. **Bewusst weggelassen** (Cargo-Cult für ein Solo-Projekt, Justin so beraten): CI/CD-Zwang pro Commit, Unit-Test-Gate pro Skill, Komplett-Rewrite. **Verifiziert ohne Server** (Windows, kein GX10/MC hier): `node --check` sauber, Import-Kette löst auf, **20/20 Logik-Selbsttests grün** (Taxonomie/Erfolgs-Checks/Reflexe). **Ehrlich offen:** noch **nicht** gegen einen Live-1.21.11-Server gefahren (Eval braucht laufenden Server + op'd `BertaEval`); echter Befund nebenbei aufgedeckt: `GoalCollect` verwechselt Block- und Item-Name (in `FAILURE_MODES.md` dokumentiert — die Eval tut also, was sie soll). **nexus** clean, nur dieser Log-Eintrag. **Übersprungen** (sauber, ahead=0/behind=0): agentic-ai-server, JorllyJustinTransfer, PrivateBackup, rack-viewer. → [[itintech-firma]] [[mindcraft-ben-setup]]

log 26.06.2026 17:14 Claude Code (Windows / JUSTMAIN) — **/push: 1 Repo gepusht — BotV2-Recherche abgelegt.** **[[itintech-firma]] / Itin-TechSolutions** `0737fa1` (`docs:`). Neuer untracked Ordner `20_Konzepte/AIPlayGames/Minecraft/BotV2/` mit einer Datei: `research/Botv2.pdf` — Recherche-Dokument für die nächste Bot-Version (passt direkt zur `BotV1/`-Kapselung von vorhin). Auf Secrets geprüft (keine), keine Inhaltsänderung an Bestehendem. **nexus** clean (kein behind), nur dieser Log-Eintrag. **Übersprungen** (sauber, ahead=0/behind=0): agentic-ai-server, JorllyJustinTransfer, PrivateBackup, rack-viewer. → [[itintech-firma]] [[mindcraft-ben-setup]]

log 26.06.2026 16:45 Claude Code (Windows / JUSTMAIN) — **/push: 1 Repo gepusht — Minecraft-Bot in `BotV1/` gekapselt.** **[[itintech-firma]] / Itin-TechSolutions** `f693502` (`refactor:`). Der gesamte Ben-Bot-Baum unter `20_Konzepte/AIPlayGames/Minecraft/` (`code/Ben`, `config`, `docs`, `learn`, `server`, README, `.gitignore` — 278 Dateien) wurde nach **`Minecraft/BotV1/`** verschoben, um Platz für künftige Bot-Versionen zu schaffen. **Reiner Move**: Git erkennt durchgängig Renames (185↔185 Pfad-Zeilen, **0 Inhaltsänderung**), gegengeprüft per `git diff --cached -M`. Keine echten Secrets im Stage (nur `keys.example.json`, geprüft). **nexus** war **behind 4** → sauber `git pull --rebase` (Fast-Forward, keine Konflikte, kein Force), dann dieser Log-Eintrag. **Übersprungen** (sauber, ahead=0/behind=0): agentic-ai-server, JorllyJustinTransfer, PrivateBackup, rack-viewer. → [[itintech-firma]] [[mindcraft-ben-setup]]

log 26.06.2026 17:45 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 1 Repo — Minecraft-Doku doc10 ausgefüllt.** **[[itintech-firma]] / Itin-TechSolutions** `7341f9b` (`docs:`). `docs/10_Seh-Sinn-und-Bau.md` mit den Abend-Befunden ergänzt (Detail im Eintrag direkt darunter): Bau-Abschnitt → **live bewiesen** (autonomer Bot, 4 Wand-Blöcke) + adaptives buildHouse + Platzier-Erkenntnis (ersetzbare Blöcke) + Memory-Halluzination als eigentlicher Grund für Bens Bau-Aussetzer; neuer Abschnitt **„MC-Client läuft NICHT auf dem GB10"** (GL crasht/hängt über alle Backends → vom normalen PC/Mac spielen). Ben gestoppt (Justin-Wunsch). **Übersprungen** (sauber): alle anderen Repos; 4 untracked WIP-Dateien bewusst draussen. → [[itintech-firma]] [[mindcraft-ben-setup]] [[ben-vision-build-stand]]

log 26.06.2026 17:20 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 0 neue Commits (alles schon gepusht) — aber Bau live BEWIESEN + GB10-Grafik-Befund.** Nichts Neues zu committen in [[itintech-firma]] / Itin-TechSolutions (Vision/Craft/Bau schon in `d13f090`/`3f69799`/`a6e1460`); nur 4 untracked WIP-Dateien (`trainer-bot*.js`,`debug-connect.mjs`) bewusst draussen. Dieser Lauf dokumentiert die Befunde des Abends: **(1) LIVE-BAU BEWIESEN** — über viele Versuche scheiterte Ben-der-Agent am Bauen, aber **nicht am Bau-Skill**: Wurzel war eine **Memory-Halluzination** (Bens Erinnerungstext behauptete „8 Bretter", echtes Inventar via Direkt-Connect: nur leaf_litter+dirt) + Pathfinder-Drift + Kampf-Modus, der `buildWall` blockiert. Beweis dann sauber über einen **autonomen Steuer-Bot** (als „Ben", eigenes Holz gesammelt, kein Cheat): **5 oak_log → 8 oak_planks → 4 echte Wand-Blöcke** survival gesetzt (mineflayer-collectblock + bot.craft + bot.placeBlock — dieselben Primitive wie `!buildHouse`). Test-Fund: Platzier-Ziel muss auch **ersetzbare Blöcke** (short_grass) zulassen, nicht nur 'air' (sonst „kein Platz" im Wald). Gegen die Halluzination: neue Lesson „vertrau `!inventory`, nicht der Erinnerung". **(2) PRISM/MINECRAFT auf dem GB10 geht NICHT** — Justins eigener MC-Client crasht/hängt bei der GL-Fenster-Erzeugung über **alle** Backends: NVIDIA-GL = SIGSEGV in `libnvidia-glcore` 580.159.03, Zink (GL→Vulkan) = hängt, Software/llvmpipe = hängt; alle am selben Punkt nach „Backend library: LWJGL". Ursache: ARM+Blackwell-Treiber + X11 ohne echten Monitor (per AnyDesk). Akut-Fix: 2 hängende Prism-Prozesse killten den Single-Instance-Lock (Launcher öffnet wieder); aber das Spiel rendert nicht. **Empfehlung an Justin: MC vom normalen PC/Mac spielen** (Server `100.89.217.4:25565` über Tailscale), GB10 ist Compute-Box. **(3) Bewertet:** oasis-ai.org (generatives Welt-Modell, kein Agent/Server — passt nicht) und steveai.app (gleiche Kategorie wie Ben; offen ob lokal/Cloud + ob Bau gecheatet — anschauen lohnt). Ben läuft, Justus auf Wunsch gestoppt. **Übersprungen** (sauber): alle anderen Repos. → [[itintech-firma]] [[mindcraft-ben-setup]] [[ben-vision-build-stand]] [[rechner-gb10]]

log 26.06.2026 15:10 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: Ben hat jetzt AUGEN + messbar weniger Fehler + einen Bau-Befehl.** **[[itintech-firma]] / Itin-TechSolutions** (`feat:`/`docs:`, Commits `e6a801b`/`ca64499`/`d13f090`/`3f69799`). Den Nachmittag drei Dinge gebaut und **live verifiziert** (nicht nur verdrahtet): **(1) SEH-SINN — Ben sieht wirklich.** Zwei-Modell-Architektur: Qwen3.6-35B bleibt das Hirn, ein kleines VL-Modell sind die Augen. Bens Hauptmodell ist Text-only und Mindcrafts Kamera braucht headless-gl (baut auf ARM/node v26 NICHT). Lösung über Browser-Render: `addBrowserViewer` (prismarine-viewer, Bens First-Person, Port 3000) → **Headless-Chromium** (puppeteer-core + System-Chromium, SwiftShader — GPU-EGL rendert headless leider nicht) macht Screenshots → **`qwen2.5vl:3b`** via ollama beschreibt die Szene → `bots/Ben/vision.txt` → `prompter.js` injiziert `$VISION` in den Prompt. **Gemessen:** Render ~250 ms + Auge ~0.6 s warm (`keep_alive 30m`) = **~0.9 s/Blick**; bewiesen mit echter Beschreibung *„standing on a cobblestone wall with a wooden roof… no visible danger"*. Modell-Bench auf echtem Screenshot: qwen2.5vl:3b = Sieger (schnell+genau), 7b genauer aber ~5 s, moondream/granite-vision/minicpm = Müll/halluzinieren → **gelöscht** (~16 GB frei). Neu `src/agent/vision/eyes.js`, Hook in `agent.js`, `$VISION` in prompter+profile, `render_bot_view=true`, Deps prismarine-viewer/puppeteer-core/canvas. Bug unterwegs: puppeteer-screenshot ist `Uint8Array`, `.toString('base64')` liefert Müll → `Buffer.from(buf)`. **(2) FEHLERQUOTE gesenkt (bewiesen):** Ursache war `craftRecipe` (54 % Fehler = ~73 % aller Fehler — Ben craftete ohne Basis-Bretter). Fix in `skills.js`: fehlende Zutaten **rekursiv** craften (Bretter aus Holz, Tag-Rezepte) + Auto-Werkbank; `collectBlock` craftet Holzspitzhacke nach. **Gemessen: Gesamt 24 %→19 %, craft 54 %→30 %** — unter dem 20 %-Ziel. **(3) BAUEN ohne Cheat:** `!buildHouse`/`!buildWall` setzen echt Block für Block (`placeBlock dontCheat`, KEIN `/fill` — Justin will kein Cheaten); Geometrie per Dry-Run verifiziert, Live-Bau braucht genug Bretter. Dazu Wissens-Lessons (getCraftingPlan, Werkzeug→Block, Material-Kette). Doku: **`docs/10_Seh-Sinn-und-Bau.md`** neu + README/Index. **Korrektur:** ich hatte Vision anfangs als unmöglich abgetan — falsch; der Browser-Render umgeht den gl-Blocker. **Bewusst draussen:** `code/Ben/{trainer-bot*.js,debug-connect.mjs}` (WIP); Stray `library/actions.js` (mein cp-Fehler) gelöscht. Ben+Justus **gestoppt** (Justin startet neue Welt am Mac mini; Bens „NEUE WELT"-Lesson steht). **Übersprungen** (sauber): alle anderen Repos. → [[itintech-firma]] [[mindcraft-ben-setup]] [[rechner-gb10]]

log 26.06.2026 10:57 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 1 Repo — Minecraft-Ordner aktualisiert nach Ben-Grossumbau.** **[[itintech-firma]] / Itin-TechSolutions** `b0494e2` (`docs:`). Den ganzen Tag an **Ben** gebaut (alles unter `code/Ben/` schon einzeln committet; dieser Push aktualisiert nur **Doku/Ordner-Struktur**): **(1) Profi-Lernsystem** — Recherche (Voyager/Mindcraft/mineflayer-armor-manager) → **Auto-Rüstung** (`armorManager.equipAll()` war ungenutzt → Ben trägt jetzt seine Diamant/Netherite-Sachen statt an Pillagern×24/Phantomen×10 zu sterben), **Skill-Library mit Relevanz-Retrieval** (mint bewährte Befehls-Sequenzen aus Episoden, `prompter.js $SKILLS` lädt nur passende, jetzt nach Score), **zählendes Gedächtnis** (`!rememberThis` dedupt + `×N`=Wichtigkeit/Gefahr). **(2) Phase-1 „Learning Decision System"** nach einem **ChatGPT↔DeepSeek-Entwurf** (jetzt unter `learn/Brainstorming/`), ehrlich an unsere LLM-Befehls-Architektur adaptiert: `learning_stats.js` (Task-Episoden + **Utility-Score** + **Trends** „wird Ben besser?", last-10 vs prev-10, `learning_stats.json`), Skill-Scores/Confidence, Pattern (2×Fehler→Lesson), **Task-Segmentierung nach Trainer-Aufgaben** (`task_log.jsonl`) statt statischem goal-Feld. **(3) Betriebs-Stack** selbstheilend: Ben+Warmup, Trainer **Justus** (282 generierte Aufgaben, geduldig-reaktiv statt Spam, `/kill` bei Stuck via OP), keepalive, vllm-watchdog, ping-watch (Telegram-Feed+Heartbeat+Alarme, `@pingitintechbot`), chat-logger, skills-loop. **(4) Bugs:** `!restart`-Selbstkill-Schleife raus, Ertrink-Auto-Schwimmen, Stuck-Auto-`/kill`. **Server:** MC läuft jetzt auf `100.89.217.4` (= das portable Paket aus dem `server/`-Workstream, s. Eintrag unten; alter JUSTMAIN-Doc → `docs/archiv/`). **Dieser Push:** `docs/09` neu (kompletter Stand), README/07/docs-Index aktualisiert, `learn/0*.md`→`learn/25.06.2026/` (Renames), Brainstorming eingecheckt, Veraltetes nach `docs/archiv/`. **Bewusst draussen:** `code/Ben/{trainer-bot*.js,debug-connect.mjs}` (WIP), fremde `reflexion.service`-Änderung; `ROADMAP.md`-Löschung **zurückgenommen** (unklar); **`server/` unangetastet** (anderer Workstream). **Übersprungen** (sauber): Cirqit, JorllyJustinTransfer, JustUpdate, PrivateBackup, itintechsolutionswebsite, rack-viewer. → [[itintech-firma]] [[mindcraft-ben-setup]] [[rechner-gb10]]
