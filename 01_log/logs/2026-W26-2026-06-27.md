log 27.06.2026 12:49 Claude Code (Windows / JUSTMAIN) — **/push: 1 Repo gepusht — ChatGPT-Wissen auf 9 Dateien neu gebaut + NexusInput-Refresh.** **[[itintech-firma]] / Itin-TechSolutions** drei Commits: `dcde9a8` (`docs:`) = ChatGPT-Wissensdateien von 14 auf **9 konsolidiert** (README + 8 Themendateien) für **einen einzigen ChatGPT-Projekt-Upload** (Limit 10 Dateien/Vorgang); README auf die echte Mechanik korrigiert — ChatGPTs „Memory" nimmt **keine** Datei-Uploads, also **Projekt-Files** statt Saved-Memory-Paste (vorher recherchiert: nur Projects/Custom-GPT-Knowledge nehmen Dateien). `4013203` (`chore:`) = frische **NexusInput-Kopie** als Build-Quelle; **Secret-Ignore in `NexusInput/.gitignore` wiederhergestellt** (die neue Kopie hatte die Zeilen entfernt) → 3 Klartext-Token-Dateien (`APIKeys/BraveAPI.txt`, `Telegram.txt`, `Telegram-Ben.txt`) wieder per gitignore geschützt und **bewusst NICHT committet**. `9c40a8a` (`docs:`) = Minecraft `FIX-REPORT.md` (Archiv/Fail2) — liess sich erst per `git add --renormalize` stagen (autocrlf-Quirk, echte 257/204-Inhaltsänderung, kein reines EOL). ChatGPT-Desktop zusätzlich neu gestartet (Justin-Wunsch). **nexus** clean, nur dieser Log-Eintrag. **Übersprungen** (sauber, ahead=0/behind=0): agentic-ai-server, JorllyJustinTransfer, PrivateBackup, rack-viewer. → [[itintech-firma]]

log 27.06.2026 11:53 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 1 Repo gepusht — Minecraft-Reorg-Zwischenstand „roh" gesichert (Justin bewusst so entschieden).** **[[itintech-firma]] / Itin-TechSolutions** `622eb9a` (`refactor:`). Lauf mit Kontext „egal ob fertig oder nicht, einfach push". Stand war eine **halbfertige Reorg** unter `AIPlayGames/`: der getrackte `Minecraft/`-Baum **813 Dateien gelöscht** (`bot/`=Olaf, `_legacy/`, `docs/`, `research/`, nested `Minecraft/Minecraft/`, `readme.md`; auf Disk nur noch `Minecraft/server/`), daneben **neuer untracked Ordner `MInecraft/`** — **Tippfehler (grosses I)**, Geschwister neben dem korrekten `Minecraft/` — mit frischem **mindcraft-Clone (eigenes `.git`)** + `learn/` + `Archiv/`. **Ich habe vor dem Commit gewarnt** (Tippfehler-Name landet dauerhaft in der History; `mindcraft/` wird nur als **leerer Gitlink** gespeichert, die Dateien sind NICHT gesichert) und drei Optionen vorgelegt — **Justin wählte bewusst „alles roh pushen".** Secrets-Check trotzdem hart durchgezogen: `node_modules`/`keys.json` korrekt ge-ignored (root + nested `.gitignore`), zwei aufgetauchte `keys.js` sind reine Loader-Module (kein hardcoded Key, gegen-gegrept) → sauber. Ergebnis: 954 Dateien (+56918/−666), `mindcraft/` als Gitlink (`m`), gepusht `8e98b4e..622eb9a`. **nexus** war **behind 4** → sauber `git pull --rebase` (Fast-Forward `4abd1cd..39f2d5e`, kein Force, kein Konflikt), dann dieser Log-Eintrag. **Übersprungen** (sauber, ahead=0/behind=0): Cirqit, JorllyJustinTransfer, JustUpdate, PrivateBackup, itintechsolutionswebsite, rack-viewer. **Offene Empfehlung an Justin:** Tippfehler-Ordner `MInecraft/`→`Minecraft/` aufräumen und mindcraft-Nesting auflösen (sonst sichert das „Backup" den mindcraft-Inhalt nicht). → [[itintech-firma]] [[mindcraft-ben-setup]] [[botv2-minecraft]] [[mindcraft-fork-origin-403]]
