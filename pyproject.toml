[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nexus-vault"
version = "0.1.0"
description = "Nexus Vault parsing and analysis tools"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["nexus*"]