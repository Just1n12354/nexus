---
title: LOG KW 26
description: Log-Einträge für Kalenderwoche 26 (Referenz 2026: 22.06.–28.06.2026)
type: log
kw: 26
range_2026: 22.06.–28.06.2026
entries: 1
status: aktiv
updated: 2026-06-24
---

# LOG KW 26  (Referenz 2026: 22.06.–28.06.2026)

Neueste Einträge oben. Format: `log TT.MM.JJJJ HH:MM <Gerät/Persona> — <was wurde gemacht>`

log 25.06.2026 22:54 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 1 Repo, 2 Commits — Minecraft-Doku auf Leistung getrimmt.** Folgt direkt auf den 22:34-Lauf (der die Struktur eincheckte); jetzt der Inhalts-Politur-Durchgang. **[[itintech-firma]] / Itin-TechSolutions** `5f6b551` (`docs:`) — Doku konsistent + **Performance-Roadmap**. Konkret: **gpt-oss-120b/qwen3-moe-Leichen raus** (docs `02`+`05` nannten ein falsches Modell, `02` widersprach sich sogar selbst `qwen3-moe` vs `gpt-oss-120b`) → korrigiert auf das **reale `nvidia/Qwen3.6-35B-A3B-NVFP4` + `api:vllm`** (verifiziert gegen `config/andy.json` + laufenden `vllm-qwen36`-Container). **`config/README.md`** neu: `config/` war **byte-identische Kopie** von `code/Ben/` → als Snapshot markiert (Anti-Drift, code/Ben kanonisch). **`upgrades/ROADMAP.md`** neu: priorisiert nach Impact÷Aufwand — **P0** vLLM-`--max-model-len` 262k→16–32k (gibt zweistellige GiB Unified Memory frei, gegen den Swap-Druck von heute Abend) + **lessons.md-Cap** (`reflexion.js` dedupt zwar, hat aber **keinen Grössen-Cap** → jede Zeile geht in jeden Prompt); **P1** Pathfinder→Mining→Restart-Schleifen; **P2** Skill-Playbooks; **P3** systemd/Server-Umzug. **doc `08` korrigiert:** Increment 2 (Reflexions-Motor `reflexion.js`+Timer, lief real 20:14) war als „nächster Schritt" gelabelt → auf **gebaut** gesetzt. `README-Alt` als **VERALTET** gebannert; README-Struktur an Realität (learn/, logs/) angeglichen. Plus **`learn/`**: Bens selbst-generiertes Spielwissen (blocks/items/crafting/mobs/biomes) eingecheckt. — Zweiter Commit `f135a69` (`chore:`): **eigenen Fehler korrigiert** — mein erstes `git add -A` zog den vom 22:34-Lauf **bewusst ausgeschlossenen** `logs/chat/`-Runtime-Chatlog wieder rein; per `git rm --cached` + neuer `Minecraft/.gitignore` dauerhaft draussen. **Nebenbei (kein Commit):** vorher Open-WebUI-Container + `tictactoe` gestoppt → GPU **94%→1%**, Desktop entlastet; `vllm-qwen36` + **Hermes-Telegram-Gateway (Ben)** bewusst weiterlaufen lassen. **nexus** (dieser Commit, `docs:`) — nur dieser Log-Eintrag, gezielt gestaged. **Übersprungen** (sauber, ahead=0/behind=0): Cirqit, JorllyJustinTransfer, JustUpdate, PrivateBackup, itintechsolutionswebsite, rack-viewer. → [[itintech-firma]] [[mindcraft-ben-setup]] [[open-webui-gx10]] [[rechner-gb10]]

log 25.06.2026 22:34 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 2 Repos gepusht.** **[[itintech-firma]] / Itin-TechSolutions** `90dc30f` (`docs:`) — **Minecraft-Konzept restrukturiert** (passt zum Windows-Eintrag von 20:36, der die 7 flachen Doku-Dateien angelegt hatte): die flachen `01–06`-Dateien sind jetzt nach `docs/` verschoben, README als Index überarbeitet. Neu als Quelle eingecheckt: der lokale **Mindcraft-Fork „Ben"** unter `code/Ben/` (226 Dateien, 3,5 MB) — **secret-geprüft sauber**: kein `node_modules`, kein verschachteltes `.git`, **kein echtes `keys.json`** (nur das leere `keys.example.json`-Template + der `keys.js`-Loader, keine hardcodierten Tokens). Laufzeit-Konfig + LLM-Profile unter `config/`. **Bewusst NICHT committet:** `Minecraft/logs/` (einzelner Runtime-Chatlog `chat-2026-06-25.txt`) — Laufzeit-Artefakt, gehört nicht ins Konzept-Repo. **nexus** (dieser Commit, `docs:`) — war **behind 1** → `git pull --rebase` (fast-forward, konfliktfrei: frontmatter/`master.md`/`tree.md`/service-ports-Updates), dann **gezielt** gestaged (kein `add -A`): neues `20_Projekte Konzepte/justreise-persistence.md` (Geschwister von `justreise.md`), `50_Systeme/tailscale-netzwerk.md` + `acino-lohn-vorbereitung.md` (beide secret-geprüft sauber), sowie Python-Packaging `pyproject.toml` + `nexus/__init__.py` für die Luna-Parser-Module. **Übersprungen** (sauber, ahead=0/behind=0): Cirqit, JorllyJustinTransfer, JustUpdate, PrivateBackup, itintechsolutionswebsite, rack-viewer. → [[itintech-firma]] [[justreise]] [[rechner-gb10]]

log 25.06.2026 20:36 Claude Code (Windows / JUSTMAIN) — **/push: 1 Repo gepusht — Itin-TechSolutions.** **Itin-TechSolutions** `7a4c1e8` (`docs:`) — neue Konzept-Doku `20_Konzepte/AIPlayGames/Minecraft/` (7 Dateien): „lokale KI spielt Minecraft". Enthält: lokalen MC-**Testserver auf JUSTMAIN** (26.2, Java 25 nötig, `online-mode=false`, Firewall nur Tailscale `100.94.200.83:25565`, verifiziert per Server-List-Ping), Software-Stack (**Mineflayer+Mindcraft** als Körper/Brücke, **vLLM+Qwen3-MoE** als Hirn — Justin nutzt vLLM, nicht Ollama), GX10-Setup, **Versions-Kompatibilität** (Mineflayer kann 26.2 evtl. noch nicht → Server pinnen) und die Architektur-Entscheidung **Hermes-Vision+xdotool vs. Mineflayer-Protokoll** (Ergebnis: Mineflayer-Körper, Hermes bleibt Hirn). **Bewusst NICHT committet:** untracked `20_Konzepte/ChatgptMemory/` — grosser persönlicher Vault aus separatem Nacht-Lauf mit **live Secrets** (`30_Agenten/APIKeys/*.txt`: Brave/Telegram-Tokens, evtl. `Luna/config.yaml`); gehört nicht in dieses „dann push", Keys müssen erst raus. **Übersprungen** (sauber, ahead=0/behind=0): agentic-ai-server, JorllyJustinTransfer, PrivateBackup, rack-viewer; **nexus** nur dieser Log-Eintrag. → [[itintech-firma]]

log 24.06.2026 14:51 Claude Code (Windows / AcinoLaptop) — **/push: 1 Repo gepusht — PrivateBackup.** **PrivateBackup** `bf9e82d` (`chore:`) — drei 3D-Druck-Binärdateien gesichert: Stapler-Ladekabel-**Steckerhalterung** (`.3mf` neu + `.stl` geändert) und Sebastians `3Rund.stl` (neu). **Git-Auth auf dem Acino-Laptop war tot:** Identität komplett leer (musste sie repo-lokal auf `Just1n12354 <…@users.noreply.github.com>` setzen, passend zum bisherigen Autor — kein `--global`); Remotes sind HTTPS + **Git Credential Manager** ohne hinterlegtes Token, kein SSH-Key, kein `gh`, kein Eintrag im Windows-Tresor → mein Push hing **non-interaktiv 2 Min in den Timeout** (GCM kann in der GUI-losen Tool-Shell kein Login-Fenster öffnen, auch `!` nicht). **Lösung:** Justin pushte einmalig in einem **echten PowerShell-Fenster** (`4232f4a..bf9e82d`), GCM öffnete das GitHub-Login, Credential liegt jetzt im Tresor → restliche Repos pushen ab jetzt ohne den Tanz. **nexus** war **behind 2** → `git pull --rebase` (fast-forward, konfliktfrei: Ben-Agentenlog + hardware.md + Luna-MEMORY), dann dieser Log-Eintrag. **Übersprungen** (sauber, ahead=0/behind=0): Itin-TechSolutions, JustUpdate. **Offen:** `gh`/PAT als Dauerlösung und globale Git-Identität auf diesem Gerät noch nicht gesetzt. → [[claudesync]]

log 23.06.2026 15:13 Claude Code (macOS / Mac mini M4, Justinmacmini) — **/push: 1 Repo gepusht — AI Fight Club erstmals auf dem Mac mini durchgelaufen, Champion `gemma4:latest`.** **[[itintech-firma]] / Itin-TechSolutions** `e775208` (`feat:`). Justin liess den ganzen Trichter über alle 5 Ligen gegen die **20 lokalen Ollama-Modelle** (≤9.6 GB, die vom 22.06-Mac-Lauf kopierten) laufen, Schnitt **50% je Liga: 20→10→5→3→2→1**. **Champion `gemma4:latest` (Grand Final 297/300)**, Finalist `qwen3:4b` (261); gemma4 gewann jede Liga mit 100% Format. **Echter macOS-Bug gefunden + gefixt** (`engine/sandbox.py`): die Schreibschutz-conftest verglich Pfade mit `os.path.abspath`, aber macOS löst `/var`→`/private/var` als Symlink auf → der `startswith`-Check blockierte pytests **eigene `report.xml`** → **alle Coding- + Thinking-l2/l3-Slots still 0** (genau die pytest-bewerteten). Auf Linux/GB10 nie aufgetreten (kein `/private`-Symlink), deshalb lief es dort. Fix: `realpath` statt `abspath` → Engine-Selbsttest **361/361 grün** (vorher 55 rot). Drumherum: **venv mit pytest** (System-Python PEP-668-gesperrt; ohne pytest würden Coding/Thinking still 0 — dieselbe Falle wie der GB10-Fix vom 22.06, nur hier per venv statt `--break-system-packages`). Zwei neue Tools: **`tools/run_funnel.py`** (Telegram-Trichter über alle 5 Ligen mit Warmup + Resume + parametrisierbarem `--advance`) und **`tools/board_bake_macmini.py`** (mergender Baker — trägt die Ergebnisse als **Gerät „macmini"** in die Liga-Boards ein, lässt die 120 GB10/gx10-Einträge **unangetastet**). Telegram-Pings pro Modell + Liga + Champion via `@pingitintechbot` (neuer Token) an Justins Chat. **Bewusst NICHT committet:** die `Liga2-5/qualifiers.json` — mein Lauf hatte die committeten GB10-Turnier-Qualifikanten (120→60→30→15) mit den 20 Mac-Modellen überschrieben; per `git checkout` auf GB10-Stand zurückgesetzt (die echten Mac-Ergebnisse stecken in den Boards als Gerät macmini, qualifiers sind regenerierbare Laufzeit-Eingaben). **Ehrliche Einordnung:** wie beim GB10-Lauf kein Intelligenz-Ranking — der Benchmark hat eine Decke, oberhalb entscheidet Format-Disziplin; die Reasoning-Modelle (qwen3:14b, deepseek-r1:14b) verloren auf den L3-Slots an Timeouts/Rambling. Für lokale Agenten-Tasks ist gemma4/qwen3:4b damit die pragmatisch beste Wahl. **Übersprungen** (sauber, ahead=0/behind=0): JorllyJustinTransfer, JustUpdate, itintechsolutionswebsite. `results/raw` + `.venv` via `.gitignore` draußen. → [[itintech-firma]] [[telegram-pingbot]] [[claudesync]]

log 24.06.2026 19:18 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **Heimlich-Hardening von Kollege jorlly aufgedeckt + SSH-Aussperr-Falle für Justin entschärft.** Justin fragte „was wurde heute auf dem Gerät gemacht" → Auth-Log + bash_history + Datei-Mtimes geprüft. **Befund:** User `maintenance` (= jorlly, Tailscale-IP 100.109.14.39) hatte **morgens via Justins eigener SSH-Session** den User `maintenance` angelegt (`useradd`, sudo-Gruppe, **`/etc/sudoers.d/maintenance` → NOPASSWD:ALL**), **fail2ban** installiert/konfiguriert und in `sshd_config` **`PasswordAuthentication no`** gesetzt — sein Reload zielte aber auf Dienstname `sshd` statt Ubuntu-`ssh`, lief ins Leere → **der laufende Daemon lief noch mit Alt-Config** (Passwort ging weiter, Änderung nur auf Platte). Nachmittags (per Key) sich selbst in **docker-Gruppe** (≈ Root ohne sudo-Log), `speedtest-cli`+`nvtop` installiert. **Die Falle:** Platte = Passwort aus, Justin hat **keinen SSH-Key** → **nächster Reboot (stand laut Banner an) hätte Justin ausgesperrt**, jorlly per Key nicht. **Aktion (Plan A, alles reversibel):** Artefakte nach `~/Dokumente/Abklären/jorlly-backup/` gesichert (sein Key, sshd_config, ssh_config, sudoers, jail.local) + Doku `gx10-ssh-zugang-abklaeren.md`; jorllys `authorized_keys` → `.disabled` umbenannt; Temp-Passwort für `maintenance` gesetzt (Zwang zum Ändern beim 1. Login); **`sshd_config` zurück auf `PasswordAuthentication yes` + `systemctl reload ssh`** → Reboot jetzt gefahrlos. **fail2ban gelassen** (harmlos, sshd-Jail maxretry 9/10min-Ban, **keine `ignoreip`-Whitelist** für ihn → keine Selbst-Bevorzugung). **Offen/ehrlich:** jorlly hat **weiterhin NOPASSWD-sudo + docker-Gruppe** und eine **aktive Live-Session** (pts/0 seit 14:43) → Maßnahme bleibt **kosmetisch**, bis sudo/docker entzogen wird (als root dreht er alles in Sekunden zurück). Widerspricht dem bisherigen „beobachten, nicht abschotten" — Justin bewusst gemacht; sudo-Entzug + Session-Kill noch **nicht** beauftragt. → [[rechner-gb10]] [[hardware]]
 (feat: Nexus improvements — standardize frontmatter (68 files), add MOCs for 50_Systeme & 70_Arbeit, create TODO sync, improve .gitignore, add master.md & tree.md)

log 23.06.2026 01:10 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 1 Repo gepusht — AI Fight Club Turnier KOMPLETT, Champion gpt-oss:20b.** **[[itintech-firma]] / Itin-TechSolutions** `58d3e92` (`feat:`) — nach dem Benchmark-Fix von 12:25 lief der ganze Trichter **autonom durch**: Liga 2 (60) → 3 (30) → 4 (15) → 5 (Grand Final 15) → **Champion `gpt-oss:20b` mit perfekten 300/300**. Verkettung über `tools/start_league.sh` (generischer Liga-Starter: `--models ""` liest `qualifiers.json` statt aller 128; Guard zweimal nachgebessert — `\b` und dann `grep python3` matchten die aufrufende Shell selbst → jetzt `awk '$2=="python3"'`). Überwacht per ScheduleWakeup-Loop alle 15 Min (KEIN bash-Watchdog — der hatte nachts 4 Zombie-Prozesse erzeugt, weil der FERTIG-Marker nur auf Telegram ging; im 12:25-Commit gefixt). Telegram-Pings bei jedem Liga-Wechsel + Champion. **Ehrliche Einordnung (Justin fragte „warum 20b > 120b?"):** kein Intelligenz-Ranking — die Aufgaben haben eine Decke (begrenzte Coding/Writing/Tool-Tasks), oberhalb entscheidet Format-Disziplin. gpt-oss:20b lieferte fehlerfrei (300/300), gpt-oss:120b verlor 39 Punkte an genau 2 Details (writing l3 Markdown-Struktur, thinking l3 Hidden-Tests durchgefallen → 261/300). Für lokale Agenten-Tasks ist 20b die bessere Wahl (schneller + fehlerfrei), aber harte/lange Reasoning-Tasks fehlen dem Benchmark, um echte Kapazität zu trennen. **Nutzen schon real:** der Mac-mini-Lauf von 15:47 hat genau diese Liga1-Rangliste benutzt, um die 20 besten ≤9-GB-Modelle lokal auszuwählen. **Push:** Itin war **behind 20** (Acino-Wartungs-Todos + FinancePrivate/JustTodo-Backups vom Mac/Automation, alle unabhängig von AIFC) → `git pull --rebase` (kein Force, konfliktfrei). Laufzeit-Artefakte (results/raw, boards, data-jsons) via `.gitignore` draußen; committet: `start_league.sh`, Liga3-5 qualifiers, finale Board-Stände. **Übersprungen:** PrivateBackup (behind 1, fremd). → [[itintech-firma]] [[rechner-gb10]] [[claudesync]]

log 22.06.2026 15:47 Claude Code (macOS / Mac mini M4, Justinmacmini) — **JustNAS dauerhaft gemountet + 20 beste lokale LLMs auf den Mac mini kopiert.** Justin wollte dauerhaften NAS-Zugriff + die laufbaren LLMs lokal. **(1) Mount:** Synology DS923+ (`justnas`, Tailscale `100.122.172.62`, Share `JustNas`) dauerhaft nach `~/JustNAS` — SMB-User `AdminNas`, **Passwort im macOS-Schlüsselbund** (kein Klartext), Mount-Skript `~/.config/justnas/mount-justnas.sh` (holt PW aus Keychain), Auto-Mount via LaunchAgent `com.justin.justnas-mount` (RunAtLoad + 5-min-Remount). Doku in [[hardware]]. **(2) LLM-Auswahl:** NAS hat **1.4 TB / 126 Ollama-Modelle**, aber Mac mini nur **24 GB Unified Memory** (kein dediziertes VRAM) → Großmodelle (30B/70B/104B) laufen hier nicht. Nur 135 GB Disk frei, laufbare Modelle summieren 424 GB → Auswahl nötig. Als Filter die **Liga1-Rangliste** (AI Fight Club, `ways.text.total`) genutzt: **20 beste Modelle ≤9 GB** (105 GB, ~30 GB Reserve) — qwen3:4b/8b/14b, gemma3:4b/12b, gemma4, deepseek-r1:14b, cogito:8b/14b, glm4:9b, mistral-nemo:12b, dolphin3:8b, codellama:7b, llama3.1:8b, command-r7b, marco-o1:7b, granite3.3:8b, granite4:micro/tiny, llama3-groq-tool-use. Per Blob-Kopie (content-adressiert) ins lokale `~/.ollama/models` (105 GB in 21 min, ~85 MB/s). **(3) Ollama-Fix:** Brew-Paket 0.30.7 war defekt (kein `llama-server`-Runner) → `brew reinstall` auf 0.30.10, jetzt **dauerhafter Dienst** (`brew services start ollama`, Auto-Start). Verifiziert: `llama3.1:8b` → „Hauptstadt der Schweiz ist Bern" (~8s), `qwen3:14b` (9.3 GB) läuft ohne OOM. **Disk jetzt 28 GB frei (94%) — etwas knapp, im Auge behalten.** → [[hardware]] [[itintech-firma]]

log 22.06.2026 13:15 Claude Code (macOS / Mac mini M4, Justinmacmini) — **/push: 1 Repo gepusht + FinancePrivate-Datenfehler repariert.** **[[itintech-firma]] / Itin-TechSolutions** `c1bec25` — Justin hatte in JustFinancePrivate beim Anlegen des **22.06-Snapshots** versehentlich auch den **31.05.2026-Eintrag überschrieben** (25 Felder mit den heutigen Werten überbügelt, inkl. der Live-Sync-Details qty/price/last_sync von Swissquote + Trading212, die ganz verloren waren). **Fix:** den 31.05-Snapshot chirurgisch aus HEAD wiederhergestellt (`c57d516` → Privatkonto 1106.12, Sparkonto 6323, Investments zurück), 22.06-Eintrag unangetastet gelassen; verifiziert dass nur der 22.06-Block neu ist, keine globale Reformatierung, JSON valide (`cf37d4a`). Danach `chore:` App-Backup-Rotationen (JustTodo todos.json.bak* + FinancePrivate data.json.bak*) committet. Push initial rejected (Remote hatte 1 fremden Commit) → `git pull --rebase` (kein Force), dann sauber gepusht. **Übersprungen:** JorllyJustinTransfer, JustUpdate, itintechsolutionswebsite (alle clean). Randnotiz: 22.06-Snapshot hat alten `last_sync` (31.05) — Investment-Kurse ggf. neu syncen. → [[itintech-firma]] [[claudesync]]

log 22.06.2026 12:25 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 1 Repo gepusht + AI-Fight-Club-Benchmark repariert.** **[[itintech-firma]] / Itin-TechSolutions** `a0cad2a` (`fix:`) — **Benchmark-Bewertung war kaputt, jetzt korrekt.** Justin liess Liga 1 (122 lokale Ollama-Modelle) über Nacht durchlaufen; beim Prüfen fiel auf: **Coding + Thinking-l2/l3 bei ALLEN 118 Modellen 0/60** — weil **pytest fehlte**, schlug der Hidden-Test-Runner (`engine/hidden_runner.py` ruft `python -m pytest`) still fehl (kein Crash, 0/N). Decke lag bei 180/300, gpt-oss:120b gleichauf mit qwen3:4b. **Fix ohne Neulauf:** pytest installiert (`--break-system-packages`) + `requirements.txt` angelegt (verhindert Wiederholung), Liga 1 aus den gespeicherten `results/raw/`-Outputs **neu bewertet** (`tools/regrade.py`, 79 s, kein Ollama) → Schnitt **124→183**, qwen2.5-coder:32b 180→**290**, GLM/gpt-oss:120b korrekt auf 280. **Zwei weitere Bugs gefixt:** (1) `parse_files` crashte bei „Schwätzer"-Modellen (exaone-deep, lfm2.5) mit „File name too long" (Rambling als Dateiname) → kappen/mappen statt crashen; die **4 Ausgefallenen nachgewertet**, `lfm2.5:8b-a1b-q8_0` (210) kam dadurch fair in die Top 60. (2) `build_prompt` versteckte die **Eingabedatei** vor dem Modell, wenn sie als `ref` diente (toolcalling-Kopier-Aufgaben unmöglich) → im Aufgabentext genannte Inputs sichtbar lassen, echte Lösungs-Refs (`erwartet_*`) bleiben versteckt (greift für **Liga 2–5**, Liga 1 bleibt intern fair/unverändert). (3) Watchdog-Marker-Bug: Wrapper schrieb „FERTIG" nur auf Telegram, nicht ins Log → Watchdog startete endlos Neu-Läufe (4 Zombie-Prozesse aufgeräumt), jetzt `print` ins Log. **Liga 2 gestartet** mit dem korrekten **Top-60-Schnitt** (`tools/prepare_liga2.py`, deterministischer Tiebreak; `start_liga2.sh` mit `--models ""` → liest `qualifiers.json` statt versehentlich aller 128), Telegram-Pings an. **Übersprungen:** PrivateBackup (behind 1 — fremder Commit, nicht meine Arbeit; erst `pull` nötig), Rest sauber. → [[itintech-firma]] [[rechner-gb10]] [[claudesync]]
