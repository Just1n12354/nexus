---
title: LOG KW 23
description: Log-Einträge für Kalenderwoche 23 (Referenz 2026: 01.06.–07.06.2026)
type: log
kw: 23
range_2026: 01.06.–07.06.2026
entries: 18
---

# LOG KW 23  (Referenz 2026: 01.06.–07.06.2026)

Neueste Einträge oben. Format: `log TT.MM.JJJJ HH:MM <Gerät/Persona> — <was wurde gemacht>`

log 07.06.2026 22:05 Claude Code (MacBook Air M4) — **OneDrive Privat (`OneDrive-Persönlich`) komplett neu strukturiert, OneDrive-Symlinks repariert, Eingangskorb `00_InputLuna/` für Luna eingerichtet.** Ziel via `/goal`: saubere Topologie, alles sortiert. **(1) Reorg (move-only, Rollback-Snapshot vorher):** Ausgangslage zerfallen — README (Stand 30.04.) beschrieb `20_AI/30_Mail/40_Notizen/50_Bilder/60_Videos`, real weg; zwei kollidierende `10_`-Präfixe, lose Root-Ordner (`Input`, `LondonInput`, `Roboter.jpg`). **Kernerkenntnis:** diese OneDrive = umgeleitetes **Windows-„Dokumente"** des Gaming-PCs → `Pictures/` (4.3 G), `Videos/`, Game-Saves (Rockstar zuletzt 03.06. = live), `WindowsPowerShell/`, Office-Vorlagen werden von Windows-Apps (neu) angelegt → **vom Mac aus nicht verschiebbar** ohne Zerfall (war 04/2026 schon passiert). Daher 2-Zonen-Modell: 🟢 kuratiert (`10_Dokumente/` mit 01–09 inkl. neuer `06_Gesundheit`/`07_Reisen/2026-06_London`/`09_Diverses`, `20_KI/` aus geflachtem `10_Agenten` + `Manfred-Roboter.jpg`, `99_Archiv/`) vs 🔴 app-verwaltet (nicht anfassen, Fix gehört auf den Windows-PC). Integrität geprüft: keine Nutzdatei verloren (2118 Dateien). `Videos/` verschwand lokal **vor** jedem Move (OneDrive Files-On-Demand-Dehydrierung, nicht durch mich) → Cloud-Kopie im Web verifizieren. `Passwortliste AP1.xlsx` bewusst in Cloud belassen (Entscheid Justin), unverschlüsselt. READMEs (Root + 10_Dokumente) + `99_Archiv/_cleanup_2026-06-07.md` + Manifest geschrieben. **(2) Symlinks:** `~/onedriveprivat` war **toter Symlink** (Ziel `OneDrive2-FreigegebeneBibliotheken–Onedrive` weg) → auf `OneDrive-Persönlich` umgebogen; redundanter `~/OneDrive`-Symlink gelöscht. Schema jetzt sauberes Paar `~/onedriveprivat` + `~/onedriveacino`. **(3) `00_InputLuna/`** (Root-Dropbox, `00_`-Präfix oben): Eingangskorb für Telegram-Bot **Luna** zum Abarbeiten — `README.md` (Arbeitsauftrag: sichten→benennen→ins Ziel VERSCHIEBEN→Ordner muss leer sein, Unklares = offener Task mit Rückfrage), `OFFENE_TASKS.md`, `LOG.md` (append-only, Pflicht-Eintrag pro Durchlauf). Fertig-Prompt mit Pfad an Justin zum Senden an Luna übergeben. **Offen/ehrlich:** README ist Anweisung, kein Automatismus — ohne Ordner-Watcher→Luna-Trigger arbeitet es sich nicht selbst ab; und damit Luna es *dauerhaft* weiss, gehört Pfad/Ablauf in `30_Agenten/Luna/memory.md`, nicht nur in einen Chat-Prompt. nexus-Doku (`storage-cloud.md`) auf neue Struktur + Symlink-Fix aktualisiert. → [[storage-cloud]] [[itintech-bots]] [[hardware]] [[game-saves]]

log 07.06.2026 22:04 Claude Code (MacBook Air M4) — **Arcera/Acino-OneDrive komplett neu strukturiert (via /goal) + Downloads aufgeräumt.** Saubere nummerierte Topologie durchgezogen statt neu erfunden: neue `10_Maschinen/Linie_05/` konsolidiert (vorher zersplittert über `Linie5/` + `Störrung/Linie 5/` + `Marchesini/`), Naming vereinheitlicht (`Linie10`→`Linie_10`), neue Ebenen `30_Kommunikation/Teams/` (zwei doppelte Teams-Ordner zusammengeführt) + `50_Berichte_Offerten/`, `Trockenschrank`/loses PDF/`Bilder` aus Root einsortiert, leere OneDrive-Default-Ordner (Attachments/Documents/Meetings/Recordings) entfernt. **Move-only, Cirqit unangetastet (27178 Dateien verifiziert), Manifest `_REORG_MANIFEST_2026-06-07.json` (60 ops).** **Blocker entdeckt: OneDrive-Download tot** — alle Files `dataless`, materialisieren nicht (`md5`/`dd`→Timeout trotz laufender App+Netz). Folgen: (a) Duplikate nicht per Hash beweisbar → **quarantäniert statt gelöscht** (`Archiv/_Quarantaene_2026-06-07/`, 7 Dateien), nur `.lnk`-Müll hart gelöscht; (b) ItinTech-Docs nicht aus OneDrive raus (Cross-Volume=Download) → in `_RAUS_ItinTech_2026-06-07/` gestaged. **Offen bis Sync wieder lädt:** Daemon neu starten → Quarantäne hashen+löschen, `_RAUS_ItinTech/`→`Itin-TechSolutions/`. Upload-Sync läuft aktiv (FileProvider-Logs aktuell), aber Status vom Terminal nicht hart verifizierbar (Binär-Logs) → GUI-Icon prüfen. **Downloads:** 3 private PDFs (Manteca-Reservierung + London-Zusammenfassung → `onedriveprivat/.../07_Reisen/2026-06_London/`, Claude-Befehle.pdf → `09_Diverses/`), Downloads geleert. → [[storage-cloud]]

log 07.06.2026 17:40 Claude Code (MacBook Air M4) — **nexus-Härtung in 6 Schritten autonom durchgezogen (via /goal), alles committet + gepusht.** Auslöser: ehrliches Audit fand strukturelle Probleme statt Kosmetik. **(1) Linter vertrauenswürdig:** `check_vault.py` DRIFT-Grace (30d, ignoriert Bulk-Commit-Rauschen → DRIFT 42→6), `_lint-report.md` vom Linting ausgeschlossen (Self-Scan-Bug: append-only Report zitierte Befunde → flaggte sich selbst), Konsole trennt blockierend (FM/ORPHAN) von advisory — „64 Befunde" waren real nur 3 Stopper. **(2) Persona-Guard:** neuer `check_persona_guard.py` blockiert Löschen/Ausweiden von `type: persona`-Prompts + Bot-`memory.md` (Anlass: Voll-Purge löschte luna.md+memory.md, an denen der Telegram-Bot hängt). **ZWEI Wurzeln gefunden, warum aller Schutz nie lief:** `core.hooksPath` war nie gesetzt UND der Hook war nicht ausführbar (Mode 100644) — git überspringt nicht-+x-Hooks still. Beides gefixt, Persona-Guard live blockierend verifiziert. **(3) Korpora getrennt:** Obsidian `userIgnoreFilters` blendet die 6 `quellen/`-RAG-Korpora (~270 Dateien) aus Graph/Suche. **(4) london entkoppelt:** `london.md` war durch Read→Write-Unfall mit `N|`-Zeilennummern-Präfix korrupt (267 Zeilen) → repariert (Inhalt verifiziert intakt), check_vault + gen_tree_index schließen london-App-Daten aus (per .gitignore nur london.md getrackt), `justreise.md`-Pfad auf `Itin-TechSolutions/.../JustReise` korrigiert. **(5) Obsidian-UI-State** (workspace/graph.json) gitignored + untracked (verursachten Cross-Device-Konflikte). **(6) Backup verifiziert:** NUR Git-Replikation + GitHub, KEIN restic/NAS/Time-Machine — 3-2-1 nicht erfüllt, als einzige nicht-aus-dem-Vault-behebbare Lücke markiert (dringend: Time Machine + restic→NAS). **Nebenbei:** korrupte Git-Index-Datei (nur 4 statt 588 Dateien im Index) entdeckt + aus HEAD repariert — sie hatte meinen Schritt-2-Commit auf 4 Dateien getrunkt; seither Tree-Verifikation vor jedem Commit + `GIT_DISABLE_INDEX_MMAP=1`. → [[improvements]] [[storage-cloud]] [[personas]] [[justreise]]

log 07.06.2026 08:31 Hermes (Telegram) — **London/JustReise/Hermes-Status gesichert vor geplantem Mac-mini-Neustart.** Für den Harry-Potter-Tag den Ablauf nach Nutzerkorrekturen präzisiert: **Chinatown-Abendessen als erledigt** markiert, **Uber £40** von **„gemeinsam / noch zuzuordnen“ auf Justin** umgebucht und im **Zeitplan auf den 07:29-Slot** verschoben (statt pauschal Rückweg), inkl. Updates in `london.md`, `london-tagesplan.json` und `london-finanzen.json`. Live-/Daten-Layer gegengeprüft. Zusätzlich **Hermes von 0.15.0 auf 0.16.0 geupdated**, `approvals.mode` auf **off** gesetzt (weniger Genehmigungs-Prompts) und Gateway-Neustart angestossen. Nutzer macht danach den **Mac-mini-Neustart** selbst. → [[london]] [[JustReise]] [[services-mac-mini]] [[hermes-agent]]

log 06.06.2026 18:47 Claude Code (Justinmacmini) — **Alle 7 Aktiv/-Projekte auf einheitliche Standard-Topologie standardisiert (README/CHANGELOG/TODO + docs/src/data/config/tests/archive/temp), Live-Dienste nachgezogen, committet & gepusht.** Ziel via `/goal`: jede App unter `Itin-TechSolutions/20_Projekte/10_Apps/Aktiv/` identisch strukturieren, kein Müll im Root. **Vorgehen risikobasiert** (erst analysieren: 3 Dienste laufen direkt aus Repo-Pfaden → Entscheidung „voll standardisieren + Live nachziehen"). **(1) JustWebsite:** V2→`src/`, V1→`archive/`. **(2) JustUpdate** (Windows-Build-Pipeline): PS-Skripte→`src/`, `.iss`/`.ico`/`VERSION`→`config/`, `Releases/`→`archive/`, `build.ps1`+`.iss` auf `$root`=Projektwurzel + `src/`/`config/`/`temp/`/`archive/`-Pfade gezogen, veralteten `07_Software/JustUpdate`-Pfad gefixt (Windows-Build muss 1× re-verifiziert werden). **(3) JustLauncher** (Multi-Plattform): Mac/Android/Windows/RaspberryPi + AppIcon→`src/`, **~114 MB Build-Müll** (Mac `build/`+`dist/`, Android `build/`+`.gradle/`+`.kotlin/`, `__pycache__`) entfernt → 3.7 MB. **(4) JustReise:** Code+`templates/static`→`src/`, `Dockerfile`/`compose`/`requirements`/`plist`→`config/` (Build-Context=Wurzel), Daten extern → kein `data/`-Move; Runtime-Klon `~/JustReise` unberührt. **(5–7) Flask-Apps mit `__file__`-relativen Pfaden** — Kern-Herausforderung: Code nach `src/`, Daten nach `data/` bricht `BASE_DIR/datei` → in jeder App `ROOT_DIR`/`DATA_DIR` eingeführt + Pfadkonstanten + Auto-`git add` umgestellt: **JustTodo** (LIVE :8070, `todos.json`+`todo/`→`data/`), **JustFinanceBusiness** (LIVE :8090, `data.json`/`quittungen`/`rechnung`→`data/`, eigene `/rechnung`-Route ergänzt da statisches Web-Root-Serving wegfiel, `qr_bank.png`+`index.html` bleiben Web-Root `src/`), **JustFinancePrivate** (LIVE :8080, komplexester: 13 Daten-Ordner inkl. `Bank/` 624 Dateien/`steuern/` 214 + echte Finanzdaten → `data/`, generisches File-Serving mit `data/`-Fallback + `..`-Schutz, Backup-Route→`ar... [truncated]

log 06.06.2026 18:15 Claude Code (Justinmacmini) — **JustLauncher als native Mac-App gestartet, falsches Dock-Icon behoben, Build-Stolperfalle in der README dokumentiert.** **(1) App öffnen:** „justlauncher öffnen" — es gibt kein `.app`-Bundle, sondern der Flask-Server läuft als LaunchAgent (`com.itintechsolutions.justlauncher`, Port **5050**, `KeepAlive`) plus einen nativen **pywebview**-Wrapper `JustLauncherMac/justlauncher_app.py`. Erst per `python3 justlauncher_app.py` gestartet — Fenster ging auf, aber das **Dock zeigte das generische Python-Raketen-Icon** statt des JustLauncher-Icons. **(2) Ursache:** nicht die `.icns` (geprüft via `iconutil` → korrekt, rote Rakete auf Schwarz, alle Größen 16–512@2x da), sondern: ein per `python3` gestarteter Wrapper erbt das Icon des Python-Interpreters; das App-Icon kommt nur über das gebaute Bundle. **(3) Fix:** `bash build.command` → `dist/JustLauncher.app` (38M) via PyInstaller; das ad-hoc Codesigning scheiterte mit `resource fork, Finder information, or similar detritus not allowed` (extended attributes am Bundle) → `xattr -cr dist/JustLauncher.app` + `codesign --force --deep --sign -` → `flags=0x2(adhoc)`, `bundle_identifier=ch.itintechsolutions.justlauncher`, `JustLauncher.icns` korrekt in `Contents/Resources/` + Info.plist. `open` → Dock zeigt jetzt das richtige Icon. **(4) README:** neuer Abschnitt **„Native Mac-App (Desktop-Wrapper)"** in `JustLauncherMac/README.md` — Warnung „NICHT per `python3` für den Alltag starten", richtiger Build-Weg, der Codesign-`xattr`-Fix als Pflichtschritt nach jedem Build, Icon-Cache-Hinweis. **Nicht committet.** Schließt thematisch an die JustReise-Ausgliederung an (JustLauncher = reines Dashboard). → [[justlauncher]] [[services-mac-mini]] [[service-ports]]

log 06.06.2026 18:13 Claude Code (Justinmacmini) — **JustReise: Citymapper-Fix, LondonInput-Docs (Tickets/Schritte/Belege) eingebunden + umbenannt, Schritte-Feature auf den Mini deployed; nebenbei 3 Finanz-/Todo-Dienste wiederbelebt.** **(1) Citymapper-Fix** (Ziel wurde beim Antippen nicht ausgefüllt): Ursache — die Map-Links sind Google-`?q=Name+volle+Adresse+PLZ`-Strings ohne Koordinaten, Citymapper füllt das Ziel aber nur mit `endcoord` (lat,lng); das bisherige Browser-Geocoding scheiterte an den unsauberen Queries (Nominatim: 0 Treffer auf den vollen String). Neu serverseitige Route **`/api/geocode`** (richtiger User-Agent gem. Nominatim-Policy, **Query-Vereinfachung** voll→ohne PLZ→ohne PLZ+Hausnr→Name+London, persistenter Cache `nexus/20_Projekte/london/london-geocode.json`); Client ruft eigene Route statt Nominatim direkt (same-origin, kein CORS) → URL enthält jetzt `endcoord` → Ziel gesetzt. Live verifiziert (Hotel → `51.5226,-0.0841`). **(2) LondonInput-Docs:** neue Unterordner `Ticket/`+`Schritte/` (plus `JustinGeld/`/`SarahGeld/`) tauchten nicht auf, weil `_onedrive_input_docs()` nur die oberste Ebene scannte → **rekursiv** + je Unterordner eine eigene Doc-Gruppe (🎫 Tickets / 👟 Schritte / 🧾 Belege); `_find_london_doc` + `_london_search_dirs` suchen jetzt auch in den Input-Unterordnern; `#`/Leerzeichen im Dateinamen brach den Link (Fragment) → Template-`href` per `|urlencode`. **`OSError EDEADLK`-Fix beim Doc-Serving:** OneDrive lieferte die Files als online-only/`dataless` Platzhalter, werkzeugs `sendfile` crasht darauf → Doc-Route liest die Datei jetzt **selbst in den Speicher** (umgeht sendfile, materialisiert) + `brctl download`-Retry-Fallback. Wichtige Erkenntnis: der **LaunchAgent-Prozess darf Cloud nicht synchron materialisieren** (bekommt sofort EDEADLK statt zu blockieren) → alle 13 Dateien einmal aus interaktiver Shell materialisiert (0 `dataless`). **(3) Umbenennung** aller 13 LondonInput-Dateien auf einheitliches `YYYY-MM-DD_<Kategorie>_<Detail>[_N]`: Tickets (`Ticket_Natural-History-Museum` = WPTools-Ticket, `Bestaetigung_Natural-History-Museum`/`Bestaetigung_Sherlock-Holmes-Museum` = Gmail-Confirmations; NHM-Ticket+Mail teilen Order-Nr S16924060), `Schritte_1–4`, `Beleg_Justin_1–3`/`Beleg_Sarah_1–3`. **(4) Schritte-Feature live:** eine Parallel-Session hatte im Repo (MacBook) aus den Samsung-Health-Screenshots ein Fitness-Feature gebaut (`LONDON_STEPS`+`LONDON_STEPS_GOAL`, `_steps_summary()`, `ksep`-Jinja-Filter Schweizer Tausender `’`, neue Sektion **06 Schritte**) — fehlte auf dem Mini, weil die plist `~/JustReise` (separater Runtime-Klon) startet, nicht das Repo. Repo war sauberer **Superset** (Schritte-Feature **+** meine Fixes) → Backup nach `~/JustReise/backups/`, 3 Dateien Repo→`~/JustReise` kopiert, `launchctl kickstart -k`. Live verifiziert: Sektion 06 Schritte (66’824 gesamt, Ø 16’706/Tag, Ziel 6’000), Nummerierung 06→12, `/api/geocode`+Doc-Serving weiter HTTP 200. **(5) Nebenbei:** JustFinancePrivate (8080) war down (404), weil nach der Apps-Struktur-Vereinheitlichung die plist noch auf alten `…/app/server.py` zeigte; Compat-Shims (via iCloud angekommen, re-execen den echten `server.py`) + `launchctl kickstart -k` → `finance`/`financeapp`/`justtodo` alle wieder HTTP 200. **Endziel offen:** `_deploy/reload_launchagents.sh --apply` am Mini → plists auf neue Pfade, dann Shims löschen. **Nicht committet.** Schließt an den 10:11-Eintrag an. → [[justreise]] [[london]] [[services-mac-mini]] [[service-ports]] [[financeapp-privat]] [[justtodo]]

log 06.06.2026 10:11 Claude Code (JustinMacAirM4) — **London-Reise als eigene App `JustReise` (Port 8060) ausgegliedert, JustLauncher zum reinen Dashboard reduziert, Redesign live, Reise-Daten unterwegs gepflegt.** **(1) Extraktion:** der gesamte London-Teil aus `JustLauncherMac/app.py` (war noch monolithisch, 1886 Zeilen, inkl. `LONDON_TEMPLATE`-String, `LONDON_*`-Daten, `/london*`-Routen) per Build-Skript chirurgisch in ein **neues Projekt `Aktiv/JustReise/`** mit sauberer Topologie zerlegt: `app.py` (Logik/Routen, Pfade von `/london`→`/` rebased, `render_template` statt `render_template_string`, Port 8060, public `/api/v1/health`), `reise_data.py` (alle Reise-Daten), `templates/reise.html` (Jinja-Layout als echte Datei), `static/`, `README.md`, `Dockerfile`, `docker-compose.yml`, LaunchAgent-Plist. **(2) JustLauncher bereinigt** (1886→560 Zeilen): alle `LONDON_*`/`/london`-Reste raus (`/london`→404), ungenutzte Imports getrimmt, **AIWorld-Karte entfernt** (auf Justins Wunsch), London-Trip-Karte → **JustReise** (zeigt auf `:8060`). **(3) Deploy-Gotcha iCloud:** beide Apps liefen auf dem Mac mini direkt aus dem **iCloud-gesyncten Repo-Klon** — JustReise crashte mit `OSError EDEADLK` beim Laufzeit-Lesen von `templates/reise.html` (frisch gesyncte/dataless Datei); JustLauncher überlebte das früher nur, weil sein Template ein Inline-String war (kein File-Read). **Lösung:** beide Runtimes per `rsync` in **nicht-iCloud-Ordner** `~/JustReise` bzw. `~/JustLauncher` deployed, LaunchAgents (`com.itintechsolutions.justreise` neu, `…justlauncher` umgebogen) darauf gezeigt → Repo = Quelle, `~/App` = Runtime. **(4) Verifiziert live:** `:8060/`+`/todos`+`/dayplan`+`/doc/*`+`/manifest` alle HTTP 200; `:5050/` 200, `/london` 404, Karten korrekt; über Tailnet `justinmacmini…:8060` bestätigt. **(5) Redesign:** vorbereitetes `templates/reise-redesign.html` → aktives `reise.html` getauscht (alte gesichert), redeployed, per Unique-Marker `wx-skel` in der Live-Seite verifiziert. **(6) Reise-Daten unterwegs** (Justin im Hotel London, via Live-API gepflegt, geteilt mit Sarah): Checkliste abgehakt `streetart`/`bricklane`/`towerbridge` (Fehl-Markierung `bigben`/`nhm`/`soho` wieder geöffnet — sind für heute geplant); heutiger **Tagesplan Sa 6.6.** gebaut: Shake-Zmorgen → Natural History Museum → Big Ben → Borough Market (Lunch) → ganztägige Sidequest „Obdachlosen was Gutes tun" → Chinatown-Dinner. **Flag:** iCloud synct das ganze Repo **inkl. `.git`** zwischen MacBook & Mini → fragil (Index-Korruption sichtbar `D`/`MM`, `EDEADLK`); Empfehlung Repo aus iCloud raus. **Nicht committet.** Schließt an den 09:49-Eintrag an (gleiche Migration, hier vollzogen). → [[justreise]] [[justlauncher]] [[london]] [[services-mac-mini]] [[service-ports]] [[network-tailscale]]

log 06.06.2026 09:49 Claude Code (JustinMacAirM4) — **JustLauncher von allen London-Resten bereinigt (Reise wird eigene JustReise-App).** Hintergrund: der London-Reise-Tab wird aus JustLauncher herausgelöst und zur separaten **JustReise**-App (Port 8060); JustLauncher soll wieder reines Service-Dashboard sein. Code war schon zu ~90 % migriert (alte `/london`-Routen + `LONDON_*`-Konstanten längst raus, Karte heisst bereits `JustReise`, static, Port 8060), aber Reste blieben: **(1) `JustLauncherMac/app.py`** — 4 Kommentare „z.B. London" → „z.B. JustReise". **(2) `JustLauncherMac/README.md`** — dokumentierte noch die **komplette** alte `/london`-Seite (Routen, `LONDON_*`-Datenmodell, Doc-Serving, Upgrade-Anleitung), also Doku für Code der nicht mehr existiert → ganze Sektion entfernt, durch knappe „Statische Karten (JustReise)"-Erklärung ersetzt (MagicDNS-Host/extern-Öffnen/kein LaunchAgent); london-Endpoints aus der Endpoint-Liste raus. **(3) Android `MainActivity.kt`** — toter `isLondon`-Sonderfall (`/london*` → extern) entfernt; greift nicht mehr, da JustReise auf anderem Host liegt und ohnehin als extern erkannt wird. **(4) `.gitignore`** Kommentar „London-App" → generisch. **(5) Gelöscht:** `JustLauncherMac/backups/` (alte London-App-Snapshots + rohes OneDrive-Import-Staging mit Buchungs-PDF/Wise-/WhatsApp-Geld-Screenshots — vorher verifiziert dass alles sauber benannt in `nexus/20_Projekte/london/` liegt, also redundant; Ordner war ohnehin gitignored) + stale `__pycache__/`. Verifiziert: `grep -rin london` über den ganzen JustLauncher-Ordner = **0 Treffer**, `app.py` kompiliert sauber, Windows-/Pi-Wrapper waren schon clean. JustReise-Karte bleibt als normaler Service-Link drin. Nicht committet. → [[justlauncher]] [[london]] [[services-mac-mini]]

log 05.06.2026 09:35 Claude Code (JustinMacAirM4) — **Vault-Haltung eingeführt, AIWorld-Bot-Config-Bugs gefixt, Luna aus AIWorld entfernt.** **(1) HALTUNG:** neue `_meta/HALTUNG.md` (ehrlich/direkt/kein Schleim, verbindlich für jede KI) als Bootstrap-Schritt 0 verdrahtet — in [[TREE]], [[README|_meta/README]], [[ROUTING]], [[CONVENTIONS]] + in den Session-Start-Hook (`~/.claude/scripts/session-start.sh`, injiziert Haltung direkt in jede Claude-Code-Session) + als Verweis-Zeile in die 4 AIWorld-Bot-Prompts (claudio/felix/frank/bruno). Dabei veralteten `Struktur`-Block im Hook gefixt (zeigte noch `20_Arbeit`/`30_Projekte`/`70_Agenten`). **(2) bots.json-Pfad-Bug** (war offen seit Reorg, geloggt 03.06): alle `system_prompt_file`/`memory_dir`/`cwd`/`add_dirs` zeigten auf totes `20_Agenten/` → `30_Agenten/` (14×, alle 4 Bots luden aus totem Pfad); Felix `add_dirs` `10_Apps/JustFinance*` → `10_Apps/Aktiv/JustFinance*`. Backups daneben, JSON validiert. **(3) Luna aus AIWorld entfernt:** Luna-Objekt aus `bots.json` (5→4 Bots, `aiworld.py` lädt dynamisch → kein Crash). Folder-Voll-Purge gestartet, dann via Git wiederhergestellt, weil der **separate** Telegram-Bot `@Lunaitintech_bot` ([[itintech-bots]]) seine Persona aus `30_Agenten/Luna/luna.md`+`memory.md` lädt — beide waren `compressed,dataless` (cloud-evakuiert, daher `EDEADLK` in der Luna-Session), `git checkout` hat sie frisch materialisiert → jetzt gesund/lesbar, Telegram-Reload sicher. ~20 Doku-Stellen von „5 Bots" auf 4 korrigiert (READMEs, ROUTING, INDEX/TREE auto, personas, tools, services-mac-mini, service-ports, profile, aiworld, AIWorld-quellen-cheatsheets) + Luna-Handoffs aus den Hub-Bot-Prompts entfernt. Linter grün, nicht gepusht. → [[HALTUNG]] [[aiworld]] [[itintech-bots]] [[_registry/personas]] [[services-mac-mini]]

log 03.06.2026 21:31 Hermes (Telegram) — **London-Tagesausgaben aus Revolut-Screenshots für Justin & Sarah erfasst.** Justin sichtbar: **£91.19 gebucht** (`The Joiner on Worship` £61.20, `Tst-Hungreek` £14.49, `Fwd Coffee Ltd` £15.50) + **£43.00 ausstehend** (`The Luna Entertainmen` £3, `Three` £40) + **£9 Vorautorisierung** (`Pointahotel Shoreditch`) + **£0 Kartenprüfung** (`TFL`). Sarah sichtbar: **£56.08 ausstehend** (`ALDI` £3.08, `The Mundo Pixar Exhib` £29.95, `Black Sheep Coffee` £23.05) + **£0 Kartenprüfung** (`TFL`). **Zusammen aktuell sichtbar ohne 0-GBP-Prüfungen und ohne Hotel-Vorautorisierung: £190.27**. → [[london]]

log 03.06.2026 13:20 Claude Code (Justinmacmini) — **London-Tab: neue Buchung integriert + dynamische „aktuell-relevant-zuoberst"-Ansicht.** **(1) Mundo Pixar:** neue PDF aus OneDrive `OneDrive-Persönlich/LondonInput` (Mundo Pixar Experience, Mi 3.6. 15:00, Wembley HA9 0TF, 2× Adult £62, gebucht über Universe für Sarah) → kopiert nach `nexus/20_Projekte/london/Aktivitaet_MundoPixar.pdf`, als Aktivität + Reiseplan-Highlight + Dokument eingebaut; Quelldatei in OneDrive vom GUID-Namen auf `Aktivitaet_MundoPixar.pdf` umbenannt. **(2) Dynamische Reihenfolge** (client-seitig, Geräte-Uhrzeit, kein Server-Neustart nötig): neue `LONDON_EVENTS`-Liste (zeitverankerte Termine, `|tojson`); Hero-Card „⏭️ Als Nächstes" unter Countdown (nächster Event ≥ jetzt, Mini-Countdown, blendet nach Trip-Ende aus); Sektionen in `<section class=mod data-mod>` → JS sortiert Live-Sektionen (flug/hotel/akt/itin) nach frühestem zukünftigem Event-Anchor (itin im Trip auf „jetzt"), Referenz-Blöcke (todo/spots/tips/docs) unverändert danach, Refresh 60s; Aktivitäten nach `data-start` (vergangene ausgegraut ans Ende); Reiseplan vergangene Tage ausgegraut, „Heute" grün. **(3) Timing-Fix:** Mundo Pixar = fixer Termin 15:00, Hotel nur Check-in-Fenster → Check-in-Event auf 18:30 (nach Pixar) → Hero/Reihenfolge zeigen Pixar zuerst. Live verifiziert (13:17: Hero=Mundo Pixar, Reihenfolge `itin→akt→hotel→flug`). Commits `1ef3155` + `0720864`, gepusht. **Offen:** AIWorld/Luna `bots.json` zeigt noch auf `20_Agenten/` statt `30_Agenten/` (nach nexus-Umbau kaputt) — Luna lädt nicht; Felix `add_dirs` ebenfalls auf alten `10_Apps`-Pfad. → [[london]] [[services-mac-mini]]

log 03.06.2026 Claude Code (Justinmacmini) — **JustLauncher London-Tab perfektioniert: öffnet im Browser + geteilte To-Dos.** **(1) PDF-Tap-Bug:** Auf dem Android-Wrapper blieb die London-Karte in der WebView hängen (öffnet nur fremde Hosts extern, London liegt aber auf Dashboard-Port 5050) → WebView kann keine PDFs rendern → Tap tat nichts. Fix ohne App-Neubau: London-Karte verlinkt über **Tailscale-MagicDNS-Host** (`justinmacmini.tail8b5081.ts.net:5050`) statt nackter IP → Wrapper wertet als „extern" → öffnet in Chrome, wo PDFs rendern. `build_services()` schreibt `static`-URLs nicht mehr auf den Request-Host um; Android-`MainActivity.kt` zusätzlich sauber gefixt (`/london*` immer extern, greift nach App-Build). **(2) To-Dos zentral statt localStorage:** auf Wunsch (Sarah hat auch Zugriff) jetzt serverseitig auf dem Mac mini — `GET`/`POST /london/todos` → `todo_state.json` (in `london/.gitignore`, bleibt aus Git raus), 15s-Polling → Justin & Sarah sehen denselben Stand. **(3) Politik:** Doc-Links ohne `target=_blank`, `tel:`-Hotel-Link, „Heute"-Markierung im Reiseplan, PWA-Meta-Tags. **(4) README** neu: LaunchAgent-Deployment, Pfad-Gotcha, London-Datenmodell + Upgrade-Anleitung, MagicDNS-Erklärung, To-Do-API. Itin-TechSolutions committet & gepusht (`dfff09b`). → [[london]] [[services-mac-mini]] [[network-tailscale]]

log 02.06.2026 23:08 Claude Code (Justinmacmini) — **LaunchAgent-Pfade nach Aktiv/-Move gefixt + JustLauncher London-Seite ausgebaut.** **(1) Services:** nach dem Verschieben der Apps in `20_Projekte/10_Apps/Aktiv/` zeigten die 4 LaunchAgent-`.plist` in `~/Library/LaunchAgents/` noch auf die alten Pfade (`/10_Apps/Just…` ohne `/Aktiv`) — liefen nur als alte Prozesse weiter, hätten Reboot nicht überlebt. `finance`/`financeapp`/`justlauncher`/`justtodo` gepatcht (`/Aktiv` eingefügt, Backups `*.bak_20260602`), via `bootout`+`bootstrap`+`kickstart` neu geladen → alle 4 laufen aus neuem Pfad, HTTP 200 (Ports 8090/8080/5050/8070). **Offen:** AIWorld (Port 9000) läuft als Altprozess, sein `.venv` fehlt → kommt bei Neustart nicht hoch (separat). **(2) JustLauncher `/london`:** `LONDON_DIR` zeigte ins Leere (`nexus/London`) → auf `nexus/20_Projekte/london` korrigiert; Doc-Links waren alle tot (altes Schema `Hotel.pdf`…) → auf echtes Schema umgestellt, kategorisiert (19 Dateien, alle ausgeliefert getestet). Seite massiv ausgebaut: Live-Countdown bis Abflug, Flug-/Hotel-/Aktivitäten-Details, Reiseplan, ~30 Spots als Grid, interaktive To-Dos (localStorage), Tipps. Hotel-Tür-PIN aus Source entfernt (Verweis auf PDF). Itin-TechSolutions committet & gepusht. → [[london]] [[services-mac-mini]] [[git-playbooks]]

log 02.06.2026 22:55 Claude Code (JUSTINGAMINGPC) — **nexus Struktur/Wartung (Checkliste-Punkte 5–30) + alle GitHub-Repos gepusht.** **(1) Struktur 5–12:** Nummern-Kollision gelöst — nexus `20_Agenten`↔`30_Projekte` getauscht, sodass nexus `20_Projekte` = Platte `20_Projekte` (456 Renames, 44 Pfad-Refs umgeschrieben, Cross-Repo-Namespace-Tabelle in [[TREE]]); `aktiv/`-Unterordner aufgelöst (Projekte flach, Aktiv/Archiv via `status:`-Frontmatter); Master-Files `_master-projekte`/`_master-referenz` nach `archive/` (status: archiv) + 3 Sektionen extrahiert ([[windows-toolchain]], [[game-saves]], [[claude-session-hook]]). **Wartungs-Toolkit `_tools/`** (nexus-hardening v0.3.0): `gen_tree_index.py` (TREE/INDEX-Autogen), `check_vault.py` (Frontmatter/updated-Drift/verwaiste Wikilinks/Min-3-Links), `check_append_only.py` (archive+historical eingefroren, 00_Log append-only), `check_stale.py` — alle im erweiterten **pre-commit-Hook**. **(2) Wartung/Doku 17–30:** Lern-Loop-Messung in [[learning-loop]] + „Tailnet ≠ Internet" als harte [[CONVENTIONS]]-Regel; [[errors-log]] triagiert; [[next-steps]] auf P0/P1/P2+Datum (inkl. Security-Härtung aus [[security-status]] = Punkt 4); [[improvements]] gegen Checkliste dedupliziert; neue Notizen [[service-ports]], [[glossar]], [[projekt-template]], [[storage-cloud]], [[health-heartbeat]] (Konzept 23/25); ROUTING-Lücken geschlossen; Owner-pro-Ordner in [[_registry/personas]]. **27/30 erledigt** (23/25 Konzept, 24 = JustUpdate-Rollout offen). **(3) GitHub:** auf Wunsch **alle 5 Repos** gepusht — `nexus` (1 Commit, ganze Session) + `Itin-TechSolutions` stückweise (Move `10_Apps/*`→`Aktiv/*`, JustFinancePrivate 212 MB in 4 Teilen gebatcht, **alle Finanz-/Personendaten bewusst inkl.**, + JustKarma); JustUpdate/PrivateBackup/itintechsolutionswebsite waren schon synchron. Git-Identität `Just1n12354 <j.itin@gmx.ch>` lokal gesetzt. → [[improvements]] [[learning-loop]] [[git-playbooks]] [[next-steps]] [[CONVENTIONS]]

log 02.06.2026 10:50 Gaming PC Sarah — SCUM-Savegame wiederhergestellt. Slot-1-DB (`Savegame/Scum/1/Saved/SaveFiles/SCUM.db`) war beschädigt → lud nicht. Aus dem Git-Backup-Repo `PrivateBackup` (privat) den Commit `9c22cc4` (Stand 01.06. 23:48) genommen und den **gesamten Scum-Ordner** zurückgespielt → **Slots 1–4 laufen** (Slot 4 bestätigt), ~1 Std Verlust akzeptiert. Kaputte DB gesichert: `C:\Users\Justin\SCUM_BROKEN_backup_2026-06-02.db`. Auto-Sync spiegelt den Live-Ordner und hat **Slots 5–7** wieder entfernt (kein Verlust: liegen weiterhin in Commit `9c22cc4`). Stand 1–4 committet & gepusht.

log 02.06.2026 10:40 Gaming PC Sarah — London-Reise (3.–9.6.): Dokumente im Ordner `30_Projekte/london` aufgeräumt & einheitlich umbenannt (Flug_/Hotel_/Aktivitaet_/Pass_/ETA_/Versicherung_/Checkliste_). Passdaten gegen Pässe + UK-ETAs geprüft → alles konsistent (Justin Pass X6915559 bis 31.05.2032 / ETA bis 21.03.2028; Sarah Pass X0H15C15 bis 11.03.2036 / ETA bis 04.04.2028). Online-Check-in Hinflug erledigt (Bordkarten da, Sitze 9E/9F, Boarding 06:35, Heathrow T5). Git: erst alles ignoriert, dann auf **ausdrücklichen Wunsch ALLE Dokumente inkl. Pass-Scans + Passnummern (Klartext)** ins private Repo `nexus` committet & gepusht (Risiko bewusst akzeptiert; bleibt in History). Offen: GetYourGuide Sky-Garden-Voucher (GYGX7NFN4MHF) noch laden · Rückflug-Check-in Mo 8.6. ~19:45. Siehe [[london]].

---

## Log-Navigation

← [[KW_22/LOG|KW 22]] · [[_log-index|↑ Log-Index]] · [[KW_24/LOG|KW 24]] →
