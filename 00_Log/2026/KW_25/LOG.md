---
title: LOG KW 25
description: Log-Einträge für Kalenderwoche 25 (Referenz 2026: 15.06.–21.06.2026)
type: log
kw: 25
range_2026: 15.06.–21.06.2026
entries: 5
---

# LOG KW 25  (Referenz 2026: 15.06.–21.06.2026)

Neueste Einträge oben. Format: `log TT.MM.JJJJ HH:MM <Gerät/Persona> — <was wurde gemacht>`

log 21.06.2026 21:36 Claude Code (macOS / Mac mini M4, Justinmacmini) — **/push: 1 Repo (Itin-TechSolutions), 4 Commits.** `cffc8ec` (`build:`) — **alle 8 Launcher-/Web-Services dockerisiert + auf ARM64 verifiziert** (JustTodo/JustBetter/FinanceBusiness/FinancePrivate/JustReise/JustLauncher/JustWebsite-nginx/JustSavegame): 5 neue Dockerfiles, 2 kaputte gefixt (JustReise fehlte `JUSTREISE_PORT=8060`; JustLauncher hatte `EXPOSE 5050` statt 8000, kopierte `api_auth.py` nicht und zog Mac-natives pyobjc rein → jetzt nur Flask). `5cbec2b` (`feat:`) — **[[projectnorm]] optimiert**: `Doku/SETUP.md` als fester Standard-Teil (agent-first „ZUERST LESEN" = welche Software/Versionen, damit alle Geräte gleich) — `init` legt sie an, `validate` prüft sie, AGENTS-Essenz verweist drauf, gemeinsamer Basis-Stack (Docker/Python 3.12/Git) als Tool-Konstante `COMMON_STACK`; `pytest.ini` in `ALLOWED_ROOT_FILES`. `6b6cb29` (`docs:`) — **16× `Doku/SETUP.md` + AGENTS-Essenz-Sync** über alle 10_Apps. `084b680` (`chore:`) — **AIEyes 14/14 sauber** (pptx-Versionslücke geschlossen), **AI Fight Club 6→11/14** (README-Header+Sektionen; Entrypoint/Deck bewusst offen). **Schutz-Entscheidung:** Ordner-Versionierung (`.V0.01`) **NICHT** angewandt (Justins Wahl war „voll", ich hab widersprochen) — hätte die 6 laufenden LaunchAgents + neuen Docker-Build-Kontexte zerschossen und die `d2eab68`-Entscheidung (Suffix-Drop) rückgängig gemacht. **Session-Kontext (kein Git):** Docker Desktop 4.77 (Engine 29.5) auf dem Mac mini installiert (Homebrew-Cask scheiterte am sudo-Symlink → offizielles .dmg direkt nach /Applications); JustBetter `today.json` (Runtime) bewusst nicht committet; iCloud-`… 2.pptx`-Duplikat entfernt. Vor Push Rebase (origin behind 1, AI-Fight-Club-Commit). **Übersprungen** (sauber): JorllyJustinTransfer, JustUpdate, itintechsolutionswebsite. → [[itintech-firma]] [[projectnorm]] [[justbetter]] [[claudesync]]

log 21.06.2026 21:14 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 1 Repo gepusht.** **[[itintech-firma]] / Itin-TechSolutions** `9ae87cf` (`feat:`) — **AI Fight Club: Live-Boards + GB10-Liga-Runner**. Die 5 Liga-Boards (`AI FightClub Liga1–5.html`) live gemacht: **Poll nur bei echten Änderungen** (Signatur aus Tag+Score+Sternen statt Zeitstempel — vorher zappelte die UI alle 5 s, Justins Beschwerde), Scroll-Position bleibt; **GB10-Ergebnisse dem Gerät „ASUS GX10" zugeordnet** (`dev:"gx10"`); Filter **„Alle Geräte" entfernt** (Default jetzt gx10); neuer Katalog-Tab **„📋 LLM Liste"** = alle Modelle mit Parameter-Größe (B, real aus Ollama `/api/tags`) + GB, ohne Score; **file://-Banner** mit Live-Link (Browser blockt `fetch` auf file://). Neues Tooling in `tools/`: **`run_league_tg.py`** (Ollama-Liga gegen alle installierten Modelle, **Warmup** gegen Timeout-0er der Großen + **Auto-Unload** gegen Speicher-Wand, **Telegram-Ping** pro Kandidat via eigenem Bot, Reihenfolge **klein→groß**), **`board_live.py`** (http-Live-Bridge `graded/*.json` → `aifc-ligaN-data.json`, 5 s), **`board_bake.py`** (schreibt Ergebnisse alle 5 Min direkt ins RAW der HTMLs für file://), `roster_small_to_big.txt`. **Laufzeit-Artefakte** (results/ 1,5 MB Roh-Outputs, data-jsons, slots/) via `.gitignore` rausgehalten. Vor dem Push den Remote-Commit `19eecbc` (JustTodo Docker-Image) sauber per Rebase reingeholt (war behind 1). **Liga-1-Lauf läuft im Hintergrund weiter** (klein→groß, ~12+ Modelle gewertet). **Übersprungen** (sauber, ahead=0/behind=0): Acino, JorllyJustinTransfer, JustUpdate, PrivateBackup, itintechsolutionswebsite, rack-viewer. → [[itintech-firma]] [[rechner-gb10]] [[claudesync]]

log 21.06.2026 20:56 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **/push: 1 Repo (Itin-TechSolutions).** `19eecbc` (`build:`) — **JustTodo-Docker deploybar gemacht**: echter Image-Name `ghcr.io/just1n12354/justtodo` statt `<user>`-Platzhalter, Healthcheck via `/api/v1/health`, optionaler Key-Mount für den Mac, Health-Endpoint meldet echten Port statt 8070. Image lokal gebaut (197 MB, amd64). **Offen:** ghcr-Login + Multi-Arch-Push (buildx), dann Cutover auf dem Mac mini. 4 Repos sauber übersprungen. → [[itintech-firma]] [[claudesync]]

log 21.06.2026 20:19 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **/push: 0 Code-Repos gepusht — alle sauber; nur nexus committet + gepusht (offener Docker-Log + dieser Lauf).** Inventar: 6 Repos gescannt, **5 sauber** (ahead=0/behind=0, nichts dirty) → übersprungen: agentic-ai-server, Itin-TechSolutions, JorllyJustinTransfer, PrivateBackup, rack-viewer. Einzig **nexus** dirty: der **19:49-Docker-Log-Eintrag** (reines Logging, von paralleler Session) lag noch uncommittet in `KW_25/LOG.md` → mit diesem `/push`-Eintrag zusammen gezielt per Pfad gestaged (kein `git add -A` im nexus), als `docs:` committet und sauber gepusht. Keine Code-Änderung, kein Blocker, kein Secret. → [[itintech-firma]] [[claudesync]] [[rechner-gb10]]

log 21.06.2026 19:49 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **Docker-Konzept angelegt + Docker Desktop auf Gaming-PC installiert + JustTodo als ersten Container real getestet.** Auf Justins Frage „wie mach ich jede App als Docker, damit sie auf allen Geräten läuft": neues Konzept **`Itin-TechSolutions/20_Projekte/20_Konzepte/Docker`** in der App-Topologie (`README.md`, `Doku/Anleitung.md`, `PowerPoint/`). **PowerPoint-Deck** `Docker_Konzept_v0.001.pptx` (9 Slides) per `build_deck.py` generiert — exakt im Stil der App-Konzepte (Default-Office-Theme, 16:9, Title-Slide + Title-and-Content, **bewusst ASCII** ohne Umlaute wie die JustTodo/JustLauncher-Decks). **Kernaussage ehrlich:** Justins „ein PC ist anders"-Problem ist die **CPU-Architektur** (Gaming-PC x86 vs Mac mini/GX10/NAS ARM), nicht nur das OS → Docker allein reicht nicht, braucht **Multi-Arch-Build (`buildx`) + Registry (ghcr.io empfohlen)**; **GPU (RTX 3090) bleibt am Gaming-PC**, nicht containerverschiebbar. **Docker Desktop v4.78.0 (Engine 29.5.3, WSL2, linux/x86_64)** per winget installiert, lief ohne Reboot. **JustTodo-Pilot** (`10_Apps/JustTodo/Code`): `Dockerfile` + `docker-compose.yml` + `.dockerignore`, Image gebaut (197 MB) und **wirklich getestet** — Container hoch, Daten-Volume gemountet, **HTTP 200 + Titel „JustTodo"**, danach Test-Container entfernt. Erststart-Falle dokumentiert (credential-helper `docker-credential-desktop` nicht im PATH → Build-Abbruch; Fix: `…\resources\bin` in PATH). Commits gingen über den parallelen 19:48-/push-Lauf hoch (`30f7a81`). Justin zudem einen fertigen **Mac-mini-Prompt** gegeben (Docker Desktop ARM64 via brew-cask/dmg, Engine-Poll, hello-world-Test). **OFFEN:** Multi-Arch real nicht getestet (Build lief nur amd64), **ghcr.io noch nicht aufgesetzt** → Mac/GX10 können das Image noch nicht ziehen; das ist der nächste echte Schritt. **NICHT gepusht** (reines Logging). → [[itintech-firma]] [[claudesync]] [[rechner-gb10]]

log 21.06.2026 19:48 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **/push: 1 Repo gepusht (Itin-TechSolutions).** **[[itintech-firma]] / Itin-TechSolutions** `c4c1062` (`feat:`) — **AI Fight Club Liga-Pyramide V1**: geteilte, datengetriebene Bewertungs-Engine (`checks`/`grade`/`sandbox`/`hidden_runner`), Turnier-Trichter **120→60→30→15→Champion** (`standings`/`promote`/`funnel`) und **Ollama-Runner** (`tools/run_league.py` + `.ps1` zum Start auf dem GB10, Live-Ausgabe). **5 Ligen × 30 Aufgaben** (5 Disziplinen × 3 Level × 2 Varianten), jede per **Golden-Musterlösung verifiziert** (golden→10, empty→0); Liga 1+2 handautoriert (5 Disziplin-Subagenten), **Liga 3–5 deterministisch generiert** (`tools/gen_liga345.py`, Erwartungswerte aus Referenz berechnet). Schwierigkeit steigt zweifach: härtere Aufgaben **und** Sternkurven-Exponent (1.0→1.6). **361 Tests grün**, perfektes Modell 300/300 je Liga. Engine aus `neu/Liga1` auf geteilte Wurzel gezogen, `neu/` entfernt. Dazu Justins **2 Docker-Commits mitgepusht** (`30f7a81` — JustTodo-Docker-Pilot + Docker-Konzept-Deck). Remote wuchs während des Laufs mehrfach (paralleles GB10/Gerät) → jeweils sauber `git pull --rebase` + Push. **nexus**: voriger 19:0x-Lauf hatte LOG-Konflikt (Parallel-Agenten) → abgebrochen/übersprungen; inzwischen extern synchronisiert, dieser Lauf trägt nur diesen Eintrag nach. **Übersprungen** (sauber, ahead=0/behind=0): agentic-ai-server, JorllyJustinTransfer, PrivateBackup, rack-viewer. → [[itintech-firma]] [[claudesync]] [[rechner-gb10]]

log 21.06.2026 18:12 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **/push: 1 Repo gepusht (Itin-TechSolutions), nexus lokal committet aber Push BLOCKIERT (divergent).** **[[itintech-firma]] / Itin-TechSolutions** 2 Commits sauber gepusht (`f8dbe63..f58528f`): **`f870fc5` (`feat:`) — AI Fight Club: editierbares Liga-Board + KI-Eintragung.** Das Single-File-Board (`AI FightClub Liga1.html`, von Justin als „fast beste HTML" gewählt) zu einer Vorlage umgebaut: alle Ergebnisse genullt/entfernt, **Eintragen-Maske** statt 66k-Zeichen-JSON-Frickeln (Sterne 0–10 je Skill/Level → Score/Klasse/Rang rechnet das Board selbst), **Auto-Save in localStorage**, Export „Als HTML speichern"/JSON + Import, **Geräte-Filter RTX 3090 / ASUS GX10 / Mac Mini**, riskanter „Leeren"-Button auf Justins Wunsch entfernt. **3 KI-Eintragwege, gleiches Schema:** `window.AIFC` (JS-API), `aifc_enter.py` (CLI, `--liga N`, 15-Sterne-CSV), JSON-Import — dazu `KI-ANLEITUNG_Eintragen.md`. **5 getrennte Liga-Boards** (Liga1–5, eigener localStorage-Key + Label je Liga). Dann **`f58528f` (`refactor:`)** — auf Justins Umbau das alte `alt/`-Verzeichnis gelöscht und den Inhalt auf die AI-Fight-Club-Wurzel gehoben (Liga1–5, engine/, engine_tests/, CLI, Anleitung flach). Beide Commits Secret-Scan sauber, 11.7k Dateien als Renames/Deletions. **WICHTIG — Credentials gehen wieder:** entgegen dem 16:55-Eintrag von heute funktioniert `git push` auf JUSTINGAMINGPC nun (beide Pushes ohne GUI-Prompt durch). **nexus BLOCKIERT:** divergent **ahead 2, behind 3** (die 2 lokalen Commits vom 16:55-Lauf + dieser Log = 3 lokal; Remote hat 3 GB10-Commits voraus). Push wäre non-fast-forward → **NICHT gepusht**; erst `git pull --rebase` nötig, dabei **LOG.md-Konflikt wahrscheinlich** (Remote-GB10-Einträge betreffen dieselbe KW_25-Datei). **Übersprungen** (sauber, ahead=0/behind=0): agentic-ai-server, JorllyJustinTransfer, PrivateBackup, rack-viewer. → [[itintech-firma]] [[claudesync]]

log 21.06.2026 16:55 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **/push: 0 Repos gepusht — 2 nexus-Commits LOKAL, Push BLOCKIERT (GitHub-Credentials auf diesem PC abgelaufen).** **nexus** `dbdf9ec` (`docs:`) — neue Wissensbasis **`10_Personen/privat/`** angelegt: `justin.md` (volles Personenprofil — Kurzprofil/Geburtstag/Beziehung, Arbeitsprinzipien, Acino-Kontext + Kontakte, [[itintech-firma]], Hauptprojekte GameZeroOne/CS50x/Finanzen, Hardware-/Rack-Setup, Agentenrollen Luna/Claude Code/Ben/Hermes) und `sarah.md` (Partnerin: Profil, Gaming-PC-Setup, gemeinsame Planung/Geschenke/Reisen). 476 Zeilen, **gezielt pro Pfad gestaged** (kein `git add -A` im nexus), **Secret-Scan sauber** (beide Dateien führen Passwörter/Tokens/Keys explizit als »nicht speichern« — nichts Sensibles drin). **Kontext /pull:** rack-viewer + nexus sauber per rebase gepullt (»Already up to date«). **4 Repos beim Pull NICHT erreicht** (agentic-ai-server, Itin-TechSolutions, JorllyJustinTransfer, PrivateBackup): in der nicht-interaktiven Agent-Shell kann der **git-credential-manager keine GUI öffnen** → kein gecachtes Credential, Pull bricht ab; alle 4 sind aber sauber (ahead=0/behind=0, nichts dirty) — keine offene Arbeit, Justin kann sie bei Bedarf im eigenen Terminal pullen. (Zwei parallel gestartete Pull-Loops hatten sich anfangs am Credential-Manager gegenseitig verklemmt + ~30 hängende git/GCM-Prozesse erzeugt → alle sauber gekillt, dann sequentiell wiederholt.) **Push-Blocker:** kein gültiges GitHub-Credential auf JUSTINGAMINGPC — git-credential-manager müsste eine GUI öffnen (in der Agent-Shell nicht möglich), `gh auth status` meldet **Token von Just1n12354 ungültig/abgelaufen**. Beide nexus-Commits liegen lokal (`git log` ahead 2); Justin muss einmal `gh auth login` (oder `git push` im eigenen Terminal mit GCM-Popup) machen, dann gehen sie hoch. → [[itintech-firma]] [[claudesync]]

log 21.06.2026 16:42 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 1 Repo gepusht.** **[[itintech-firma]] / Itin-TechSolutions** `ddf42f6..b9fd107` (2 Commits): **`889efa3` (`feat:`) — AI Fight Club auf Geräte-Slots umgebaut** (Justins Wunsch „Slot für 3090 und x10"): `slots/3090` (Snapshot des Original-Laufs) + `slots/x10` (Live-Symlink auf den GB10-Benchmark-Baum), neues `board_devices.py` + Template = Vergleichs-UI 3090 vs x10 (Score + tok/s je Modell), `run_all_parallel.py` (Speicher-Budget-Scheduler, mehrere Modelle gleichzeitig dank 121 GB Unified Memory) + `run_ways.py` (native+text Bewertungsweg). **`b9fd107` (`feat:`) — AIEyes Linux/X11-Strang**: `device.py` kennt GB10, `Code/linux/` Aktions-Schicht (xdotool/ImageMagick, 10/10 Selbsttest) + Sandbox-Orakel auf Xvfb :99. Dabei 3 versehentlich (Cleanup-Glob) gelöschte 3090-Daten (`gpt-oss_20b_Coding`) vor dem Commit wiederhergestellt. Laufend im Hintergrund: NAS→GB10-Modell-Kopie (~1,5 TB) + GB10-Benchmark (Score+Speed). **Übersprungen** (sauber, ahead=0/behind=0): Acino, JorllyJustinTransfer, JustUpdate, PrivateBackup, itintechsolutionswebsite, rack-viewer. → [[itintech-firma]] [[rechner-gb10]] [[claudesync]]

log 21.06.2026 09:42 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 2 Repos gepusht.** **[[itintech-firma]] / Itin-TechSolutions** `ddf42f6` (`feat:`) — neues **`/pull`-Kommando** in [[claudesync]] scharf geschaltet: `commands/pull.md` (geräte-weiter Pull per `git pull --rebase --autostash`, read-only, kein Commit/Push/Log, nutzt denselben `push_scan.py`) existierte schon, war aber nicht im Soll-Register → in `manifest.json` eingetragen; `bootstrap.sh` (find-basierter Mirror) verlinkt `/pull` damit auf allen Geräten, auf gx10-bf12 sofort aktiviert (Symlink `~/.claude/commands/pull.md`, dry-run vorab geprüft). **nexus** `c72745a` — neuer Agent **Ben** aufgenommen: `30_Agenten/Ben/` (7 Dateien: Persona/System-Prompt `ben.md`, `SETUP.md`, `memory.md`, `README.md`, ARBEITSKARTE/ARBEITSLOG/NACHTSCHICHT) — Justins persönlicher Allround-Agent auf dem GB10, **Hermes Agent** (Nous Research) lokal auf Ollama `gpt-oss:120b`, erreichbar via **Telegram** `@Benitintech_bot`, voller nexus-Zugriff. Dazu `APIKeys/README.md` um `Telegram-Ben.txt` ergänzt + die **Token-Datei `Telegram-Ben.txt` bewusst mitcommittet** (auf Justins ausdrückliche Bestätigung — gleiches Muster wie die schon getrackten `BraveAPI.txt`/`Telegram.txt`, privates Repo). **Übersprungen** (sauber, ahead=0/behind=0): Acino, JorllyJustinTransfer, JustUpdate, PrivateBackup, itintechsolutionswebsite, rack-viewer. → [[claudesync]] [[itintech-firma]] [[rechner-gb10]]

log 21.06.2026 00:22 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **/push: 2 Repos gepusht.** **[[itintech-firma]] / Itin-TechSolutions** `4df133c` (`refactor:`) — auf Justins ausdrückliche Bestätigung die große (fremde) Portfolio-Konsolidierung committet: Konzepte aus `20_Konzepte/` nach `10_Apps/` (AIEyes, [[jarvis]], [[justbetter]], [[projectnorm]], Sherlock Holmes, [[swissaisolutions]]), JustKarma + Nexus raus, ganzer `20_Konzepte/`-Ordner aufgelöst (357 Dateien, −38k Zeilen). Secret-Scan sauber. **nexus — Clean-Slate-Initial-Commit** (Justins Entscheidung): forensisch festgestellt, dass die nexus-Git-History weg war (lokal `.git` heute 00:05 auf unborn `main` zurückgesetzt, `ef37cb6`/468 Dateien lokal weg, Remote `Just1n12354/nexus` 0 Branches) → auf „Clean Slate, frisch pushen" den ganzen Vault neu committet **inkl. API-Keys** (`30_Agenten/APIKeys`, bewusst, privates Repo + neues README dort). **Hirn gefüllt:** neuer Ordner `20_Projekte Konzepte/` mit INDEX + 16 Projekt-Kurzfiles (Stand/Stack/Health/Ports aus README+AUDIT+VERSIONS+PORTS). Stehengebliebenen **Merge-Konflikt-Marker** in diesem KW-Log bereinigt (beide Einträge 23:22 + 23:17 erhalten). → [[itintech-firma]] [[claudesync]]

log 20.06.2026 23:22 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **/push: 0 Repos gepusht — alle 8 Repos sauber (ahead=0/behind=0, nichts dirty).** Geprüft & übersprungen: agentic-ai-server, [[itintech-firma]]/Itin-TechSolutions, itintechsolutionswebsite, JorllyJustinTransfer, JustUpdate, PrivateBackup, rack-viewer, nexus. **Session-Kontext (kein Git — liegt auf dem JustNas/Connect-Share `Z:\Connect`):** Fernzugriff JUSTINGAMINGPC dokumentiert + verifiziert. **OpenSSH-über-[[network-tailscale|Tailscale]] war bereits eingerichtet** (frühere Session) → heute verifiziert: Dienst `sshd` Running/Automatic, Port 22, Firewall **Tailscale-only** (Regel an Interface `Tailscale` gebunden, Standard-LAN-Regel deaktiviert; kein LAN-Fallback) — Login `ssh jitin@justingamingpc.tail8b5081.ts.net` getestet. Weil Windows-OpenSSH das Windows-Konto-Passwort nutzt und das alte unbekannt war, auf Justins Wunsch das **lokale Login-Passwort von `jitin` neu gesetzt** (Admin-`net user`, von Justin selbst ausgeführt — meine Shell war nicht elevated; gilt jetzt auch für die Bildschirm-Anmeldung) und in `Verbindung.xlsx` dokumentiert (Passwort-/Status-Feld). Neue **`readme3090.md`** angelegt (analog `readmex10.md`: Gerät, Tailscale, SSH, Ollama). Falsches Rollen-Label „MAIN-PC / OpenClaw/Hermes/Android-Build" aus tree.md/xlsx auf Justins Korrektur in **„Alltags-/Gaming-PC (Hauptrechner, kein Server)"** geändert (Readme + xlsx). Dieser Log als einziger Commit. → [[network-tailscale]] [[claudesync]]

log 20.06.2026 23:17 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **Agenten entfernt — werden neu aufgenommen.** Alle 6 Agenten aus `30_Agenten` gelöscht: Claudio, Robert, bruno, felix, frank, luna (~466 Dateien). Grund: Clean Slate, die Agenten werden neu aufgebaut. `APIKeys/` (BraveAPI.txt, Telegram.txt) bewusst behalten — kein Agent. Löschung von Justin selbst auf der Platte vorgenommen; vollständig rückholbar über git (HEAD `ef37cb6`, 468 Dateien getrackt). Deletions **noch nicht committed** — gehen erst per `/push` hoch.

log 20.06.2026 18:02 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 0 Repos gepusht — alle 8 Repos sauber (ahead=0/behind=0), nichts dirty.** Geprüft & übersprungen: Acino, [[itintech-firma]]/Itin-TechSolutions, JorllyJustinTransfer, JustUpdate, PrivateBackup, itintechsolutionswebsite, rack-viewer, nexus. Auf Justins Wunsch die **offenen TODOs des Tages in die Task-App** erfasst (6): (1) BIOS „Restore AC Power Loss → Power On" setzen — letztes Glied für Auto-Hochfahren nach Stromausfall; (2) AnyDesk-Unattended-Passwort bestätigen/setzen (aus Config nicht verifizierbar); (3) NoMachine/AnyDesk auf **Tailscale-only härten** (Port 4000 lauscht aktuell auch im LAN); (4) NoMachine-Client auf Windows-PC + Macs; (5) NAS klären — justnas `.176` vs Synology DISKSTATION `.106`; (6) parallele Claude-Session prüfen. Volle Details zur heutigen Fernzugriff-Arbeit im 16:52-Eintrag. Dieser Log als einziger Commit. → [[rechner-gb10]] [[claudesync]]

log 20.06.2026 16:52 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **Fernzugriff auf den GB10 eingerichtet (Tailscale + AnyDesk + NoMachine), Auto-Login aktiv.** Justin will den Rechner unbeaufsichtigt erreichen — auch nach Stromausfall. Eingerichtet: **Tailscale** 1.98.4 (verbunden, Tailnet-IP `100.75.47.118`) + **Trayscale**-Flatpak als GUI (Linux-Tailscale hat keine native Oberfläche, Autostart gesetzt); **AnyDesk** 8.0.2 arm64 (ID `1787907288`, Dienst `enabled` + `Restart=always`/Respawn getestet, GUI-Autostart); **NoMachine** 9.7.3 arm64 (Justins Haupt-Viewer, NX-Dienst auf Port 4000, `.deb` musste per Browser geladen werden — Hotlink-Schutz blockt curl); alle drei laufen direkt über Tailscale. **GNOME-Auto-Login** für `justin` aktiv (`/etc/gdm3/custom.conf`, keine LUKS → Boot ohne Eingabe). **Offen:** AC-Power-On (Auto-Start bei Stromzufuhr) ist vom OS nicht setzbar — kein BMC (`ipmi_si` lädt nicht), muss einmal im BIOS „Restore AC Power Loss → Power On". Nebenbei: zweites NAS im LAN erkannt — **Synology DISKSTATION** `192.168.1.106` (DSM 5000/5001, SMB aktiv; nicht das von der Parallel-Session gemountete „justnas"). **(/push:** 0 Repos gepusht — Itin-TechSolutions + JorllyJustinTransfer sauber übersprungen; nexus war beim Scan dirty, wurde aber von einer parallel laufenden Session committet+gepusht, bevor ich dran war → nur dieser Log-Eintrag als eigener Commit.**)** → [[rechner-gb10]] [[claudesync]]

log 20.06.2026 16:50 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **NAS „justnas" dauerhaft als zweite Festplatte am GB10 eingehängt.** Justin wollte dauerhaften Zugriff aufs NAS wie eine lokale Platte. NAS gefunden (LAN `192.168.1.176`, Tailscale `100.122.172.62`), nur **SMB/CIFS** offen (445/139, kein NFS). Bewusst über die **Tailscale-IP** gemountet statt LAN-IP — stabile Adresse, funktioniert zuhause *und* remote, Latenz quasi gleich (1 ms vs 0,4 ms). Setup: Credentials in `/etc/cifs-justnas.creds` (root, 600; User `AdminNas`, PW hat angehängtes Apostroph), `/etc/fstab`-Eintrag `//100.122.172.62/JustNas → /mnt/justnas` mit `x-systemd.automount,nofail,_netdev` (mountet bei Zugriff, blockiert nie den Boot, übersteht Reboot), Symlink `~/justnas`, Nautilus-Lesezeichen für Sichtbarkeit im Dateimanager. Lesen+Schreiben getestet (~3,5 TB, 2,9 TB frei). Weitere Shares am NAS ungenutzt: `M2JustNas` (SSD), `docker`, `web`. **(/push:** 0 Code-Repos gepusht — Itin-TechSolutions + JorllyJustinTransfer sauber übersprungen; nur dieser nexus-Log-Commit.**)** → [[rechner-gb10]] [[claudesync]]

log 20.06.2026 06:59 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **Nacht-Rewrite gestoppt + „Ben"-Arbeiter-Workflow etabliert.** **(1)** Autonomer Nachtlauf „ganzen nexus mit `gpt-oss:120b` kompakter neu schreiben" auf Branch `nacht-rewrite` gestartet, überwacht, **nach 42 Dateien GESTOPPT**: Modell verfälschte Inhalte (`Walkthrough`→`Walkwalk`, schnitt Details weg) — kein struktureller Guardrail fängt das. `main` unberührt, 16 Versuchs-Rewrites als Beweis im Branch (löschbar `git branch -D nacht-rewrite`). read-only Audit (390/390, 0 Fehler) → `~/.nexus-audit/audit-report.md` + `MORGEN-BERICHT.md`. **(2)** Daraus Workflow neu definiert: **Claude Code = Schichtleiter, Ben = lokaler Arbeiter** (gpt-oss via deterministischer Harness, nie frei laufend). Ben macht nur kleine, begrenzte, überprüfbare Aufgaben; schreibt nie auf `main`, kein Mass-Rewrite; jede Änderung nur Branch + Diff-Kontrolle durch Schichtleiter. Spec: `~/.nexus-audit/BEN-WORKFLOW.md`. **(3)** Bens erste Aufgabe (deterministisch, `ben_linkfix.py`, report-only) lief: die **„74 toten Links" aus dem Audit deflationieren auf ~0 echte** — 110 stecken in `00_Log` (Historie, unantastbar), 3 sind Template-Platzhalter (`[[name]]`/`[[<stem>]]`), 0 saubere Auto-Fixes; einzig 2 Backslash-Tippfehler `[[acino-job\]]` in `70_Arbeit/README.md` wären manuell wert. Link-Hygiene des Vaults ist faktisch gut. **(4)** Separat abends: OpenClaw-Telegram `agents.defaults.thinkingDefault=low` (snappiger; Stack verifiziert 35 tok/s). **Kein Git-Push, kein `main`-Edit ausser diesem Log.** → [[claudesync]]

log 19.06.2026 22:35 Claude Code (Linux / NVIDIA GB10 «DGX Spark», gx10-bf12) — **/push: 0 Repos gepusht — 3 Commits LOKAL, Push blockiert (keine GitHub-Credentials auf diesem neuen Gerät).** Erster /push vom frisch eingerichteten GB10. **[[itintech-firma]] / Itin-TechSolutions — 2 Commits lokal:** `e2b596c` `feat:` gx10-bf12 in [[claudesync]] registriert (tree.md-Geräte-Sektion, `session-start.sh`-Erkennung „NVIDIA GB10/DGX Spark", `local/gx10-bf12.json` mit theme/permissions) + `8c68a63` `docs:` GB10-Hardware-Profil unter `Claudeeyes/Doku`. **nexus — 1 Commit lokal:** `6bc78cb` `feat:` Robert-Agent verbessert (neue Skills `oeffnen`+`claudeeyes`, Regeln 9/10/12 gegen Kopf-Zählen/Eigenbau/App-Code-Berg, INDEX: Re-Test Tasks 18+10 auf `gpt-oss:120b` bestanden) + dieser Log. **Push blockiert** — HTTPS-Remote, kein `credential.helper`, `gh` nicht eingeloggt, keine SSH-Keys auf gx10-bf12 → erst `gh auth login` (oder Token/SSH) nötig, dann `git push` für beide Repos. **Sauber übersprungen** (ahead=0/behind=0, nichts dirty): JorllyJustinTransfer. **Session-Kontext (kein Git):** GB10 als lokaler LLM-/Agenten-Host eingerichtet — Ollama (`gpt-oss:120b` + `GLM-4.5-Air` Q4), opencode mit Agenten robert/robert-glm, AI-Tooling (uv/aider/llm/hf/gh), Snipping-Tool (gnome-screenshot+xclip), [[claudesync]] gebootstrappt (Symlink `~/Documents→~/Dokumente` für die Hook-Pfade). **Nachtrag 22:38:** Justin hat `gh auth login` (Account Just1n12354, repo-Scope) gemacht → `gh auth setup-git` + **beide Repos erfolgreich gepusht** (Itin-TechSolutions `…→8c68a63`, nexus `…→1275c51`). git nutzt jetzt dauerhaft gh-Credentials auf gx10-bf12. → [[claudesync]] [[itintech-firma]]

log 19.06.2026 07:54 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **/push: 1 Repo gepusht.** **JorllyJustinTransfer** `ec1edee` (`docs:`) — auf Justins ausdrückliche Bestätigung die **12 [[SwissAISolutions]]-Dateien** (README/AGENTS, Doku BusinessPlan+Produktkonzept, 2 PowerPoint-Decks, research-Stubs rag/langchain/agent-configs; 952 Zeilen) aus dem Austausch-Repo **entfernt** — Konzept ist nach [[projectfusion]]/Itin-TechSolutions konsolidiert, die Transfer-Kopie war Altbestand. Beim vorigen /push (gestern) bewusst übersprungen, weil fremde Großänderung; jetzt mit OK committet. nexus zuletzt mit diesem Log. **Sauber übersprungen** (ahead=0/behind=0, nichts dirty): agentic-ai-server, Itin-TechSolutions, itintechsolutionswebsite, JustUpdate, PrivateBackup, rack-viewer. nexus-untracked `2026-06-18.md` + `Unbenannt.base` (Obsidian-Junk) weiter nicht committet. → [[SwissAISolutions]] [[projectfusion]] [[itintech-firma]]

log 18.06.2026 22:16 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **/push: 2 Repos gepusht.** **[[itintech-firma]]** / Itin-TechSolutions `a126e50` (`chore: gitignore`) — `.gitignore` erweitert: **alle ClaudeEyes-Screenshots** (`learning/**/*.png`, nicht nur `_*.png`; zeigen den echten Bildschirm → Privacy) + **Blender-Backups** (`.blend1/2`) ausgeschlossen; die 21 untracked Debug-Screenshots dieser Session + `RackPlanung/blender.blend1` damit **ignoriert statt committet**. **nexus** zuletzt mit diesem Log (fast-forward-Pull `697fede` vorher). **Vorher in derselben Session gepusht:** [[rack-viewer]] `03158e4` — Gerätenamen als **Hotspot-Schilder** auf die 3D-Geräte gesetzt (Positionen aus dem eingebetteten glb berechnet, Gravur-Optik per CSS). **Bewusst NICHT committet/übersprungen:** **JorllyJustinTransfer** — 12 **gelöschte** Dateien unter `Zusammen/SwissAISolutions/` (fremde Großänderung, NICHT von mir gemacht) → konservativ übersprungen, Justin muss bestätigen ob die Löschungen gewollt sind. nexus-untracked `2026-06-18.md` + `Unbenannt.base` (Obsidian-Junk) nicht gestaged. **Sauber übersprungen** (ahead=0/behind=0): agentic-ai-server, itintechsolutionswebsite, JustUpdate, PrivateBackup. **Session-Kontext (separate Aufgabe, kein Git):** Fernzugriff via [[network-tailscale|Tailscale]] weiter ausgebaut — auf JUSTINGAMINGPC **OpenSSH-Server** (Port 22, Firewall nur Tailscale-Interface) + UltraVNC; **Mac-mini-VNC zum Laufen gebracht**: UltraVNC scheitert an macOS (RFB 003.889) → **RealVNC Viewer** mit macOS-Konto-Auth (imjustin) installiert + verbunden, SSH-Login zum Mac verifiziert. Zentrale Doku im `JustNas/Connect`-Share: `Verbindung.xlsx` (Mac-VNC-Zeile auf RealVNC korrigiert) + `Verbindung-Setup.docx` + neu `00_NOTIZEN_FUER_KI.md` (KI-Briefing mit Zugangsdaten). → [[network-tailscale]] [[services-mac-mini]] [[itintech-firma]]

log 18.06.2026 20:55 Claude Code (macOS / Mac mini M4, Justinmacmini) — **/push: 1 Repo gepusht** (nexus mit diesem Log). **[[itintech-firma]] / Itin-TechSolutions — 2 Commits lokal, aber NICHT gepusht (behind 2, non-fast-forward):** `8dddd33` `feat: JustinV2 v0.004 — schreibende API mit Key-Auth + Wochen-History` (POST/PUT/DELETE mit `X-API-Key`, GET offen im Tailnet; CORS nur eigene Origins ohne Wildcard, `/keys` liefert Key nicht an fremde Origin → 403; Tages-Archivierung → `/api/v1/week` + Test `test_week_history.py`; `api_keys.json` korrekt via neue `data/.gitignore` ausgeschlossen, Key-Datei lag lokal aber **nicht** gestaged) und `30f69e4` `chore: JustinV2/JustTodo Laufzeitdaten aktualisiert` (today.json + neue history.json, JustTodo `todos.json.bak*`). **Push blockiert** — `git pull --rebase` nötig (kein `--force`, bewusst konservativ). **Sauber übersprungen** (ahead=0/behind=0, nichts dirty): JustUpdate, itintechsolutionswebsite. Secret-Scan sauber (einziger Treffer = Key-Generierungs-Code `secrets.token_urlsafe`, kein echter Wert). **Selbe Session vorher (separate Aufgabe, kein Git):** auf diesem Mac mini **Fernzugriff via [[network-tailscale|Tailscale]] eingerichtet** — SSH (macOS Remote Login/sshd, Port 22) + VNC (Bildschirmfreigabe, Legacy-PW, Port 5900) aktiviert und über den Tailscale-Hostnamen verifiziert (SSH-Connect OK, RFB-Banner auf 5900); Zeile `justinmacmini` in `JustNas/Connect/Verbindung.xlsx` zurückgepflegt (SSH-Plan dort von „Tailscale-SSH" auf reale „Remote Login/sshd" korrigiert). → [[justinv2]] [[itintech-firma]] [[services-mac-mini]] [[network-tailscale]]

log 16.06.2026 22:53 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **/push: 2 Repos gepusht.** **[[itintech-firma]]** / Itin-TechSolutions `cf18812` — Justins **Blender-3D-Modell** des Racks (`20_Projekte/20_Konzepte/RackPlanung/blender.blend`, 114 KB) zum bestehenden [[solomon]]-Rack-Projekt ergänzt; `blender.blend1` (Blender-Auto-Backup) **bewusst nicht versioniert** (gehört nicht ins Repo, bleibt untracked). **nexus** zuletzt mit diesem Log + dem vorigen Board-Eintrag. **Sauber übersprungen** (ahead=0/behind=0, nichts dirty): agentic-ai-server (nur „Initial commit"), itintechsolutionswebsite, JorllyJustinTransfer, JustUpdate, PrivateBackup. Keine Secrets gestaget. → [[solomon]] [[itintech-firma]]

log 16.06.2026 22:51 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **GitHub-Projektboard „Agentic AI Server" (Org [[Studio-Collado]], Projekt #3) als PoC-Projektplan befüllt.** **gh CLI** war nicht installiert → via `winget` (GitHub.cli 2.94) nachgezogen, Justin per Browser-OAuth eingeloggt (Scope `project`, User `Just1n12354`). Aus zwei Kontext-Dumps (System/Architektur + Business Plan) eine Planung gebaut: **19 Draft-Items, alle Status=Backlog** mit Size-Schätzung (XS–XL), gruppiert per Titel-Präfix `[Infra]`/`[Agent]`/`[ERP]`/`[Sales]`/`[Biz]`/`[Plan]`/`[RAG]`. Erst 16 Items angelegt, dann auf Justins „nur das Nötige"-Vorgabe statt Dubletten **2 bestehende angereichert** (Inference-Stack, Basic-Package) **+ 3 neu** (System-Architektur/Setup-Plan, Business-Plan-Draft inkl. Risk Assessment, RAG-Firmenwissen). Danach **alle 19 Beschreibungen als sauberes Markdown** formatiert (Header / Scope-Listen / Blockquotes / Emoji) — PS 5.1 zerschiesst Umlaute+Emoji beim Arg-Übergeben an `gh`, daher über **UTF-8-Bash-Skript mit heredocs** gelöst (per GraphQL gegengeprüft, Umlaute sauber). **Ehrlich geflaggt:** echter Scope-Widerspruch **„tuned model pro Kunde" vs. Phase-1 „no per customer customization"** als ⚠️-Blockquote im Inference-Item vermerkt; RAG bewusst plug-and-play formuliert (Kunde lädt eigene Docs ≠ Customization). **Offen an Justin:** Geschäftsmodell-Schritt 3 leer, kein Date-/Iteration-Feld fürs Planungs-Due-Date (Kickoff 16.06., 15 Tage), Priority-Feld ohne Optionen. Justins alter „Start 15.06.2026"-Marker ist nicht mehr auf dem Board (TOTAL=19). **Nur Board-Items angefasst, kein Repo, kein Commit.** → [[solomon]] [[privateai-appliance]] [[SwissAISolutions]] [[itintech-firma]]

log 16.06.2026 21:53 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **/push: 1 Repo gepusht** ([[itintech-firma]] / Itin-TechSolutions `887272f`; itintechsolutionswebsite, JorllyJustinTransfer, JustUpdate, nexus, PrivateBackup sauber übersprungen — alle ahead=0/behind=0, nichts dirty). Inhalt = neues Planungsprojekt **RackPlanung** (untracked unter `20_Projekte/20_Konzepte/RackPlanung/`, 13 Dateien/43 KB) committet: Hardware-Planung fürs **Digitus DN-10-05U** 10-Zoll-Rack (6 HE, innen 254×240×267 mm) für [[solomon]]/[[privateai-appliance]] — Geräte-Massblätter (ASUS Ascent GX10, Synology DS923+, Mac Mini M4, APC USV BR650MI, Switch noch offen), ein interaktives **`rack3d.html`** Three.js-Fit-Tool (Drag/Drehen, Grün=passt-rot=ragt-raus) und 5 winzige **STL-Box-Exporte** für Fusion 360. Secret-Scan sauber (nur txt/html/stl, je STL 684 B). **Nebenher in dieser Session (NICHT im Repo, liegt unter `C:\RackPlanung`):** ein **Fusion-360-Python-Script** `rack_fusion.py` geschrieben das Rack (offene Hülle) + Nutzvolumen + alle 5 Geräte massstabsgetreu als benannte Komponenten baut inkl. Fit-Check (USV 310 mm tief sprengt die 240 mm Nutztiefe → „zu gross"); Syntax via `py_compile` geprüft, ins Fusion-Scripts-Verzeichnis installiert (taucht als „RackPlanung" in My Scripts auf). **Ehrlich geflaggt:** Fusion bietet keinen externen Run-Trigger → den finalen „Run"-Klick (Shift+S) muss Justin selbst machen, ich kann das Script nicht von aussen in die laufende Fusion-Session injecten. → [[solomon]] [[privateai-appliance]] [[itintech-firma]]

log 16.06.2026 21:26 Claude Code (Windows / Gaming-PC Justin, JUSTINGAMINGPC) — **/push: 1 Repo gepusht** (JorllyJustinTransfer `8eb1a58`; nexus zuletzt mit diesem Log-Eintrag; Itin-TechSolutions/JustUpdate/itintechsolutionswebsite/PrivateBackup sauber übersprungen — Itin-TechSolutions behind 39 & JustUpdate behind 2 nur lokal hinterher, ahead=0, nichts dirty). Inhalt = **[[SwissAISolutions]]-Konzept ausgebaut** im Austausch-Repo Jorlly/Justin. **(1)** Deutsches `README.md` (Single Source, ProjectFusion V5) von 6 auf **11 Sektionen** erweitert (Pakete, Geschäftsmodell & Preise, Fahrplan, Go-to-Market, Recht & Haftung, Risiken — bullet-only, weil der Deck-Generator Tabellen/`|` als Bullets aufs Slide werfen würde) → `pptx --bump` erzeugte **Deck v0.002 mit 11 Folien**, README-Header/Changelog + AGENTS-Essenz automatisch mitgezogen, README-Snapshot in `Archiv/`. **(2)** `Doku/Produktkonzept.md` um **§13 datierten Projektplan** ergänzt (Phase 0 = 15-Tage-Planung ab 16.06., hardwarefrei, Tag-für-Tag; Phase 1+ relativ zum GX10-Tag; kritischer Pfad: Recht blockiert Echtdaten-Demo, Marke blockiert Aussenauftritt, Lasttest blockiert Paket-Zusagen). **(3)** Englischer `Doku/BusinessPlan.md` (Jorllys Skizze) zu vollem **13-Kapitel-Plan** ausgebaut (Exec Summary → Team), faktengleich mit dem dt. Konzept, CHF überall als ⚠️-Annahme markiert, „SWAT"→SWOT gefixt — auf Justins Entscheid **parallel** zum dt. Konzept (zwei Adressaten/Sprachen), nicht konsolidiert. **(4)** `design/research/` (rag/langchain/agent-configs) von leeren Stubs mit echtem Stack-Inhalt gefüllt (Qdrant/lokale Embeddings, Framework-vs-Eigenbau-Entscheid, Agent-Katalog für Pro). **Eigener Patzer offen:** beim ersten README-Schreiben rutschten zwei Schließ-Tags ans Dateiende — standen im (vom Deck übersprungenen) Changelog-Block, Folien unbetroffen, sofort rausgeputzt. **Offen bleibt (Justins Entscheidungen):** Preise gegenrechnen statt raten, Concurrency real testen, Recht-Block (AVV/AGB/BHV) vor erster Echtdaten-Demo, Domain/Marke `swissaisolutions.ch`. → [[SwissAISolutions]] [[projectfusion]] [[solomon]] [[itintech-firma]]

log 16.06.2026 15:10 Claude Code (macOS / Mac mini M4, Justinmacmini) — **/push: 1 Repo gepusht** (Itin-TechSolutions `2a0b541`; JustUpdate, itintechsolutionswebsite, nexus sauber übersprungen, ahead=0/behind=0). Inhalt + ganze Session: **(1) IH-Job-Import in [[justtodo]]:** neues `_import_ih_jobs.py` liest den Acino/Arcera-OneDrive **nur lesend** (`Offen/`/`Abgeschlossen/` + Job-PDFs) und legt **70 Todos** an (5 offen→`offen`, 65→`erledigt`), **Checkpoints aus den PDFs** als abhakbare `[ ]`/`[x]`-Checkliste im Kommentar (JustTodos vorhandene Checklist-Funktion genutzt, 41 Jobs mit Schritten); Dedup über Job-Nr (idempotent), Protokoll-Suffix „zuletzt bearbeitet…von" weggeschnitten. **(2) Wochenplanung** (`/wochenplanung`, neue Route+Seite in `server.py`): generiert **aus den offenen Todos** — Überfällig-Block, Tageskarten nach Fälligkeit, Ohne-Termin-Backlog mit „Heute/Morgen/Datum"-Verplanen (`planTo` setzt `due`), Header-Link, Acino/Privat/Alle-**Filter**. Auf Wunsch die **ganze Woche aktiv befüllt**: 13 offene Aufgaben (9 ohne Termin + 4 überfällig) per Priorität auf Heute…Mo verteilt (danach Überfällig 0 / Ohne Termin 0). **(3) Defekte LaunchAgents gefixt** (gleiche `*.V0.01`-Pfad-Drift wie zuvor): JustTodo `:8030` lieferte 500 (toter `index.html`-Pfad) → plist auf `…/JustTodo/Code/…` korrigiert, läuft (200). **(4) [[justlauncher]]:** echte **`.app` mit eigenem Icon** via PyInstaller gebaut (`build.command`), `xattr -cr`+ad-hoc-signiert, nach `/Applications/JustLauncher.app` installiert, **Login-Autostart**-LaunchAgent `com.itintechsolutions.justlauncher.app` (RunAtLoad, KeepAlive=false). **(5) [[claudesync]] auf diesem Mac mini angewandt:** `bootstrap.sh --clean` — alle `~/.claude`-Symlinks zeigten **tot** auf `ClaudeSync.V0.01/…` → re-pointet auf `ClaudeSync/Code/claude/`, `settings.json` aus base×`Justinmacmini.json` gemergt (byte-identisch, nichts verloren), Altstand reversibel in `~/.claude/_attic/20260616-150805/`; 0 tote Symlinks danach. **Offen/geflaggt:** Doku-Drift `ClaudeSync.V0.01/.V0.02` vs. realer Ordner `ClaudeSync` (nexus `repo:` zeigt ins Leere); alte ungenutzte `~/.claude/scripts/session-start.sh` (11.06). **Keine Finanzdaten/keine OneDrive-Ordner angefasst** (OneDrive nur gelesen). → [[justtodo]] [[justlauncher]] [[claudesync]] [[services-mac-mini]]

log 16.06.2026 13:52 Claude Code (macOS / Mac mini M4, Justinmacmini) — **[[justinv2]] Abnehm-Kalorienziel gebaut + alle Mac-mini-Dienste auf 80x0-Ports konsolidiert (alle live).** **(1) JustinV2-Feature:** Server (`server.py`) um Profilfelder **Geschlecht / Aktivitätslevel / Abnehm-Tempo (kg/Woche)** erweitert + serverseitige Kalorien-Berechnung: **BMR (Mifflin-St-Jeor) → TDEE (PAL-Faktor) → Defizit (7700 kcal/kg)** mit Sicherheits-Untergrenze (m 1500 / w 1200) und Halte-Modus am Zielgewicht. Neue Endpoints `/api/v1/calories` + `calorie_plan` im Profil. UI (`index.html`): Profilform um die 3 Felder, Fitness-Seite mit Aufschlüsselung (BMR/TDEE/Ziel/Protein/Prognose), „Kalorienziel heute"-Balken + Nährwert-Deltas verdrahtet (`renderCaloriePlan()`). Für Justin (90 kg, 173 cm, 23, m, leicht aktiv, 0.5 kg/Wo): **Ziel 2023 kcal/Tag**, 144 g Protein, ~40 Wochen bis 70 kg; heute 849 kcal → 1174 übrig. Verifiziert per API + py_compile. **(2) Eigener Patzer:** durch wiederholte `kickstart -k` + `SO_REUSEADDR` liefen mehrere JustinV2-Zombies parallel auf 8050 und lieferten gemischte (alte) Antworten → alle gekillt, sauber via `bootout`/`bootstrap` genau einen Prozess. **(3) Port-Konsolidierung (Justins Vorgabe):** JustFinanceBusiness 8090→**8010**, JustFinancePrivate 8080→**8020**, JustTodo 8070→**8030** (JustReise 8060, JustinV2 8050, Launcher 8000 bleiben). Befund: Env-Ports in den plists waren schon neu, aber **alle plist-`ProgramArguments` zeigten auf tote `…<App>.V0.01/Code/src/server.py`-Pfade** (Ordner waren auf nicht-versioniert umbenannt) → Dienste liefen nur als crashende Zombies (Business stürzte an fehlender `data.json`, weil die echte Datei im nicht-versionierten Ordner liegt). Fix: plists (Backup `*.bak_20260616_ports`) auf `…/<App>/Code/src/server.py` gezogen, Zombies gekillt, sauber neu gebootet. **Endstand: alle 6 Dienste live + HTTP 200** (8000/8010/8020/8030/8050/8060), Launcher zeigt alle Karten. **Offen/kosmetisch:** der `"port"`-Wert im `/api/v1/health`-Body einiger Finanz-/Todo-Apps ist noch auf den Altport hartkodiert (Bind-Port korrekt). [[services-mac-mini]] + [[service-ports]] nachgezogen. **Keine Finanzdaten angefasst** — nur plist-Config + JustinV2-Code. → [[justinv2]] [[services-mac-mini]] [[service-ports]] [[justlauncher]]

log 16.06.2026 13:17 Luna (Hermes / Mac mini) — Kurzer Gesundheits-/Alltagskontext aus dem heutigen Chat ins Gedächtnis übernommen: Justin trinkt manchmal gern Red Bull. Anschliessend die Kalorien sauber eingeordnet: **normales Red Bull ~45 kcal pro 100 ml bzw. ~112 kcal pro 250-ml-Dose**, grössere Dosen entsprechend mehr; **Red Bull Zero/Sugarfree ~0–5 kcal**. Relevant als kleine Referenz im Kontext von Justins Gewichts-Ziel, ohne das Thema künstlich aufzublasen. → [[profile]]

log 16.06.2026 11:50 Claude Code (macOS / Mac mini M4, Justinmacmini) — **[[justinv2]] live deployt + JustLauncher-Kopier-Treadmill abgeschafft + Port-Drift gefixt.** Auf Justins „mach JustinV2 live". **(1) Deploy:** LaunchAgent `com.itintechsolutions.justinv2.plist` angelegt (Port **8050**, `RunAtLoad`+`KeepAlive`), Dienst läuft — Health `{"status":"ok","app":"JustinV2","port":8050}` lokal **und** über Tailscale (`100.89.217.4:8050`), Root liefert die 40 KB-UI. **Eigener Stolperstein, sauber gelöst:** erste plist nahm `/usr/bin/python3` (= Xcode-Python ohne Full-Disk-Access) → TCC-`Operation not permitted` beim Lesen unter `~/Documents/`, Dienst-Exit 2. Auf `/opt/homebrew/bin/python3` umgestellt (hat den Zugriff, wie finance/justtodo) → grün. README-Platzhalterpfad `/Users/justin/Github/…` → realer `/Users/imjustin/Documents/GitHub/…`; Gotcha im `Code/README.md` dokumentiert. **(2) JustLauncher-Befund (Justins Frage „muss ich jedes Mal neu installieren?"):** Der Launcher startete aus einer **Deploy-Kopie** `~/JustLauncher/app.py` (alt, ohne JustinV2-Karte) statt aus dem Git-Stand → jede Änderung hätte Neu-Kopieren gebraucht. plist auf den **Git-Ordner** `…/JustLauncher/Code/src/JustLauncherMac/app.py` umgezeigt (+ `WorkingDirectory`, Backup der alten plist), Launcher neu gestartet → **JustinV2-Karte erscheint**, Restart-Button bedienbar; künftig reicht `git pull` + Neustart, **kein Copy-Deploy mehr** (wie finance/justtodo). **(3) Drift gefunden + Mac-seitig gefixt:** Launcher läuft real auf **Port 8000**, nicht 5050 wie die Doku überall sagte (Code + Live-Prozess verifiziert) → [[services-mac-mini]] + [[service-ports]] korrigiert, JustinV2:8050 in beide eingetragen, `updated:` 06-14→06-16. **PRÜFEN offen (5050 noch drin):** `security-status.md` (Firewall) + `20_Projekte/justlauncher.md` (inkl. Windows-Wrapper) — Windows-Seite von hier nicht verifizierbar, daher nur geflaggt statt blind umgeschrieben. `check_nexus.py`: 0 tote Links, 0 Datums-Drift. Gezielt pro Pfad ... [truncated]

log 16.06.2026 11:35 Claude Code (Windows / Acino-Arbeitslaptop, ACINOLAPTOP) — **/push: 0 Repos gepusht** — alle drei sauber (Itin-TechSolutions, JustUpdate, nexus; ahead=0/behind=0, nichts dirty). Heutige Arbeit lief in **OneDrive** (kein Git-Repo, daher kein Code-Push): das `Archiv` **von privaten Daten bereinigt**, weil Justin den Acino-OneDrive als Backup auf den **Firmenserver** legen will (jeder MA ein Ordner) und kein Privates drin sein soll. **135 private Dateien** in einen Review-Ordner verschoben statt gelöscht (Arztzeugnisse, **Lohnerhöhung 2026**, Marschbefehl/EO, Arzttermin-Formulare, LinkedIn, Spesen, Weiterbildungsanträge, **ItinTech**-Geschäftsunterlagen Rechnungen/Offerte/Betriebshaftpflicht, 68 Screenshots eigener Tools/Projekte) — Grenzfälle (PSA/Schutzkleidung, Pikett-Dienstplan, 3D-Druck) bewusst drin gelassen + aufgelistet. Danach auf Wunsch **303 leere Ordner** gelöscht (verifiziert: nur Gerüst, keine Dateien; 0 Cloud-Platzhalter übersehen). **Eigener Patzer offen gemeldet:** Massen-Datei-Ops auf einem **live-synchronisierenden OneDrive** ausgeführt → OneDrive hat parallel eigenmächtig umgeräumt und Dateien (auch von mir NIE angefasste wie `IH Jobs`/`Telefonsupport`/Teams-Chatdateien + den ganzen Review-Ordner) in den **Windows-Papierkorb** verschoben, kurz darauf selbst geleert → liegen jetzt im **OneDrive-Online-Papierkorb** (Cloud, ~30 T). Datenbilanz blieb sauber: alle **785** Ursprungsdateien gehen auf (625 noch im Archiv + 160 verschoben/gelöscht). Auf Justins ausdrücklichen, **gegen meine klare Warnung** bestätigten Entscheid „alles endgültig löschen": lokaler Papierkorb ist leer (von OneDrive bereits abgeräumt) — den **Cloud-Papierkorb muss Justin selbst** auf onedrive.com leeren (von hier nicht zugänglich). **Lehre gespeichert** (Memory [[storage-cloud]]): vor Massen-Ops in diesem OneDrive **erst Sync pausieren**, Erhaltenswertes lokal (Desktop, nicht umgeleitet) sichern. → [[storage-cloud]] [[user-acino-role]] [[itintech-firma]]

log 16.06.2026 11:07 Claude Code (Windows / Acino-Arbeitslaptop, ACINOLAPTOP) — **/push: 1 Repo gepusht** (Itin-TechSolutions `32be66b`; JustUpdate + nexus sauber übersprungen). Inhalt: für [[justinv2]] auf Justins Wunsch einen **hostbaren KI-Coach-Web-Prototyp** gebaut — Frontend-Shell im **JustFinancePrivate-Look** (Dark `#0B0B0F`, Crimson-Accent `#C8324C`, Sidebar/Pages, KPIs, Chart.js, 11 Konzept-Module mit Demo-Daten) unter `20_Konzepte/JustinV2/Code/src/index.html`, dazu stdlib-`http.server` auf **Port 8050** (`+/api/v1/health`) — lokal getestet, liefert aus. In **JustLauncher** (`app.py` SERVICES + neues `activity`-Icon, Crimson-Karte) und **PORTS.md** (8050 belegt, nächster Block → 8060) registriert; Deploy-Anleitung inkl. LaunchAgent-plist in `Code/README.md`. Zwei saubere Commits: `d9f1fcc` chore = reiner Ordner-Rename `JustinV2.V0.01` → `JustinV2` (lag schon vor der Session auf der Platte, pfad-stabil für den Code-Zweig), `32be66b` feat = Prototyp + Integration. **Zwei Dinge offen ehrlich gemeldet:** (1) Es ist eine **Shell ohne Backend/DB/Auth** — kein echter Coach, der V1-Stack (FastAPI/SQLite/KI) fehlt komplett. (2) Der `Code/`-Zweig **bricht die `AGENTS.md`-Regel** dieses Konzept-Ordners (»kein Code, nur Konzept«) — auf Justins ausdrückliche Entscheidung hin, als Ausnahme in `Code/README.md` vermerkt; sauberer wäre `10_Apps/` gewesen. Außerdem: Rename **droppt den Versions-Suffix** entgegen der Projektkonvention (eine `.Vx.xx`-Ordner pro Projekt) — Justin prüfen, ob gewollt. Launcher-Restart-Button für JustinV2 wirft Fehler, bis die plist auf dem Mac mini liegt. → [[justinv2]] [[justfinanceprivate]]

log 16.06.2026 10:53 Claude Code (Windows / Acino-Arbeitslaptop, ACINOLAPTOP) — **/push: 1 Repo gepusht** (nexus `60282eb`); Itin-TechSolutions + JustUpdate sauber übersprungen. Inhalt = **OneDrive-Acino-Reorg** (kein Git-Repo, daher nur die nexus-Doku im Push): den gewachsenen `Archiv\`-Müllhaufen (776 Dateien/1.3 GB, jahresbasiert + Grab-bags) auf **Jahr → Thema** umgebaut — `Archiv\<Jahr>\{10_Maschinen 20_Elektro 30_Anlagen 40_Personal 50_ItinTech 60_Admin 90_Diverses}`, **68 Move-Ops, kein Datenverlust** (alle 68 Ziele verifiziert, 0 Quellen am alten Ort; vermeintliche „6 fehlende" = `desktop.ini`-Zählartefakt). Grab-bags `Aufträge`/`Suchen`/`temp` aufgelöst; **kritische Diagnose-Daten `Störrung` aus `temp` gerettet** → `Archiv\2026\10_Maschinen\Störrung` (Maschinenpark-Index + L5-Schema). **JOB-Index-System `Offen`/`Abgeschlossen` (auto-generiert, Dateiname-gekoppelt, GxP) bewusst KOMPLETT unangetastet** — auf Justins „auch umstrukturieren" widersprochen und flach gelassen (sonst Automation-Bruch). Alles reversibel: Manifest-CSV + Undo-Skript in `Archiv\_REORG_LOGS\`. Eigener Fehler offen: hätte `storage-cloud.md` ZUERST lesen müssen — es gab ein dokumentiertes 2026-06-07-„Thema-at-root"-Design, das aber nie in die Cloud synkte; auf Justins Entscheid „Jahr→Thema behalten" wurde das 06-07-Design als überholt markiert. nexus-Pfade (`storage-cloud.md`, `acino-maschinen.md`, `acino-marchesini-mb430-schemaindex.md`) nachgezogen, Linter sauber (0 tote Links, 0 Datums-Drift). **Offen:** Sync läuft → Mac muss nach Upload abgeglichen werden. → [[storage-cloud]] [[acino-maschinen]] [[user-acino-role]]

log 16.06.2026 10:39 Luna (Hermes / Mac mini) — Hermes-Nutzung für Justin eingeordnet: `/usage` erklärt, aktuelles Limit ausgelesen und präzisiert (**Session 81 % frei / Weekly 70 % frei**, Kontext erst 12 % belegt). Danach [[justinv2]] im nexus bewusst **nur moderat** weitergeschärft statt voll ausentwickelt: neue Abschnitte **„Produktkern V1"**, **„Nicht in V1"** und **„Daten- und Vertrauensmodell"** ergänzt, damit der Konzeptfokus klarer ist (enger Startkern, bewusste Abgrenzung, Privacy/Vertrauen als Produktprinzip). Änderung live nach GitHub gepusht in `nexus` als Commit `f439de2` (`docs: sharpen justinv2 concept scope`). → [[justinv2]] [[00_Log/README]]

log 16.06.2026 09:09 Claude Code (Windows / Acino-Arbeitslaptop, ACINOLAPTOP) — **/push: 0 Repos gepusht** — alle drei Repos sauber (Itin-TechSolutions, JustUpdate, nexus; ahead=0/behind=0, nichts dirty). Heutige Arbeit lief in **OneDrive** (kein Git-Repo, daher kein Push): SIP/IH-Jobs aus ISOware verarbeitet. Den Gesamt-Export `sip-JITI01-…` mit 65 abgeschlossenen Jobs in **65 Einzel-PDFs** gesplittet (ein Job pro Seite), benannt `JOB-Nr, Aktion Objekt Beschreibung, Datum.pdf` (Windows-verbotene Zeichen `/ :` → `-`), abgelegt in `OneDrive/Abgeschlossen`. Dazu zweiten Export mit **5 offenen Jobs** in Ordner `OneDrive/Offen` gesplittet. Für beide einen Index `_Index_*.json` + `_Index_*.md` erzeugt (Job-Nr, fällig am, Aktion, Objekt, Dauer, Status, Prio, Datei; nach Datum sortiert). Auf Justins Wunsch in den **Index-Auszügen die Prio immer auf „hoch"** gesetzt — die echte SIP-Prio dabei unter `prioritaet_sip` bewahrt; SIP-System (ISOware) und Original-PDFs **nie verändert** (GMP-Datenintegrität). Auffälligkeit gemeldet: ein „offener" Job (JOB-97526053) hat Status `Arbeit erledigt`, gehört evtl. zu den Abgeschlossenen. Notiert (Memory): Acino-IH-Software = **ISOware GmbH 2026** (isoware.de), **SIP Aufträge = IH Jobs**. → [[justtodo]] [[user-acino-role]]

log 16.06.2026 00:13 Claude Code (Windows / Gaming-PC, JUSTINGAMINGPC) — **/push: 1 Repo gepusht.** [[JorllyJustinTransfer]] → `11f8cb5` docs: **[[SwissAISolutions]]** konkretisiert (auf Justins Kritik-Liste „komplett verbessern"). Im Austausch-Konzept `Zusammen/SwissAISolutions`: MVP von Kandidatenliste auf **festen Default** gezogen (vLLM + gpt-oss-120b + OpenWebUI + Qdrant) + neue §5a Demo-Mindestfunktionen und §5b Lasttest-Protokoll; Pakete Basic/Plus/Pro um konkrete Nutzer-/Doku-/Support-**Richtwerte** ergänzt (an die ~5–15-Concurrency gekoppelt); **Pilot-Paket §9a** als fertiges Angebot (8–12 Wo, Hardware Selbstkosten + Setup gratis + Abo 50 %, verbindliche Gegenleistung Feedback/Referenz); Haftung verschärft (BHV/AVV/AGB schon **vor jeder Demo mit Echtdaten**, nicht erst vor Verkauf); AGENTS.md-TODOs gefüllt; Roadmap-Reihenfolge MVP+Pilot+Recht+Preis VOR Marketing. Deck/Essenz via ProjectFusion neu erzeugt (validate 12/12). Push erst rejected (Remote war seit Scan weiter — Jorlly hatte `e9cf599` docs: business plan dazu) → sauber per `git pull --rebase` integriert (keine Überlappung, kein Force). Ehrliche Einordnung an Justin: ~6 der 10 Kritikpunkte standen schon im Konzept (Arbeit = konkret machen, nicht „fehlt"); 3 echte Blocker bleiben Welt-Hausaufgaben (Preise rechnen, GX10 lasttesten, Domain/Marke prüfen). **Blockiert:** [[justupdate]] `behind=2` → nicht gepusht, braucht erst `git pull --rebase`. Übersprungen (sauber): Itin-TechSolutions, itintechsolutionswebsite, PrivateBackup. -> [[itintech-firma]] [[SwissAISolutions]]

log 16.06.2026 00:00 Claude Code (Windows / Gaming-PC, JUSTINGAMINGPC) — **Nachtrag /push:** Justin gab grünes Licht → [[JorllyJustinTransfer]] doch gepusht (`0bfabf0` docs: SwissAISolutions-Produktkonzept für Austausch mit Jorlly). Korrigiert den vorherigen Eintrag (dort als „übersprungen, Rückfrage offen" vermerkt). -> [[itintech-firma]] [[SwissAISolutions]]

log 15.06.2026 23:58 Claude Code (Windows / Gaming-PC, JUSTINGAMINGPC) — **/push: 1 Repo gepusht (2 Commits).** [[itintech-firma]] (Itin-TechSolutions) → `13a8850` docs: neues Produktkonzept **[[SwissAISolutions]]** — aus [[Solomon]] ein verkaufbares Produkt gemacht: ASUS Ascent GX10 als Plug-&-Play On-Premise-KI („privates ChatGPT") für Schweizer KMU (Treuhand/Kanzlei/Praxis), Wert = Appliance-Image + Onboarding + Managed-Service-Abo (MRR), nicht die Hardware-Marge; Pakete Basic/Plus(RAG)/Pro(Custom). Ordner nach **ProjectFusion V5** standardisiert (README=Single Source → Deck+AGENTS generiert, Detailkonzept in `Doku/Produktkonzept.md`, validate 12/12). Ehrlich geflaggt: nie „100 % sicher" versprechen (nur „on-premise/CH", Einzelfirma haftet privat), ~5–15 Nutzer/Box, Preise sind Annahmen. `8852244` docs: Solomon-Ordner aufgeräumt (120b-Modelltest-Artefakte entfernt, Kaufentscheid/Anforderungskatalog/Pipeline-Decks ergänzt). Secret-Scan sauber. **Bewusst NICHT gepusht:** [[JorllyJustinTransfer]] hat eine Kopie des SwissAISolutions-Konzepts (inkl. interner Preis-/Haftungsannahmen) in `Zusammen/` — Teilen mit Jorlly ist outward-facing & schwer rückholbar → übersprungen, Rückfrage an Justin offen. **Blockiert:** [[justupdate]] `behind=2` → nicht gepusht, braucht erst `git pull --rebase` (kein Force). Übersprungen (sauber): itintechsolutionswebsite, PrivateBackup. -> [[itintech-firma]] [[SwissAISolutions]] [[Solomon]]

log 15.06.2026 22:58 Claude Code (Windows / Gaming-PC, JUSTINGAMINGPC) — **[[Solomon]]: ASUS Ascent GX10 (4 TB) gekauft.** Justin hat den NVIDIA-GB10/DGX-Spark-Rechner auf Digitec gekauft — 4-TB-Variante (ASUS Ascent GX10) für **CHF 3'924 statt 4'217**, 128 GB unified Memory. 4 TB statt 2 TB (CHF 3'602) wegen +322 CHF Aufpreis = drei Upgrades: mehr Modell-Headroom ([[AI Fight Club]]), PCIe 5.0 statt 4.0 und **TCG-Opal-Hardwareverschlüsselung** statt Pyrite (relevant für sensible Finanz-/Steuerdoks). Zweck: lokaler 24/7-Inferenz-/Tuning-Knoten für große MoE-Modelle wie **gpt-oss-120b (~45–50 tok/s, ~60–65 GB MXFP4)**, die die RTX 3090 (24 GB) nicht laden kann. In dieser Session entstanden in `20_Projekte/20_Konzepte/Solomon`: Anforderungskatalog (MoSCoW: MUSS 100 %, SOLL 94 %, KANN 20 %), Pro/Contra-Deck, **24/7-Agent-Pipeline-Konzept** (ehrlich: assistierte Pipeline mit Sandbox+PR+Eval-Gate, KEIN unbeaufsichtigtes Self-Training → Model-Collapse-Risiko) sowie PPTX-Build-Skripte. Offen: Aufstellung/Rack — Tendenz geschlossener 10-Zoll-Schrank (~312 mm, DIGITUS) mit Dachlüfter, Material Alu (2020-Profil) wegen Hitze/Brandschutz; Hitze-Caveat dokumentiert (GX10 läuft heiß, 273 GB/s Bandbreite = Schwäche nur bei *dichten* 70B). → [[Solomon]] [[AI Fight Club]] [[itintech-firma]]

log 15.06.2026 22:04 Claude Code (Windows / Gaming-PC, JUSTINGAMINGPC) — **Token-/Kostenverbrauch von Claude Code ausgewertet.** Justin fragte, wo er seinen Token-Verbrauch sieht (mit Screenshot `Downloads/Screenshot 2026-06-15 215537.png` der Desktop-App-Nutzungsseite). Klargestellt: die App zeigt nur %-Plan-Limits (Sitzung 18 %, wöchentlich alle Modelle 45 %, Sonnet 0 %), keine echten Tokens. Echte Zahlen via `npx ccusage@latest monthly` gezogen (liest lokale CLI-Logs, nur dieser PC): **Juni 2026 ~1,13 Mrd. Tokens (~$897 API-Listenwert)**, Mai 2026 ~521 Mio. (~$465), Summe ~1,65 Mrd. / **~$1.362**. Ehrlich eingeordnet: Cache-Read dominiert (~97 %), die $-Beträge sind hypothetischer API-Preis (nicht das, was Justin via Max-Pauschale zahlt) und NICHT gleich Anthropics reale Compute-Kosten — die sind intern/unbekannt, grob geschätzt ein Bruchteil. Nur Claude-Code-CLI auf diesem Gerät erfasst, App/Web nicht. → [[claudesync]]

log 15.06.2026 21:53 Claude Code (Windows / Gaming-PC, JUSTINGAMINGPC) — **/push: 1 Repo gepusht.** [[itintech-firma]] (Itin-TechSolutions) → `d2eab68` refactor: Versions-Suffix `.V0.01` aus allen Projektordnern unter `20_Projekte` (Apps/Konzepte/Friedhof) entfernt — Git erkennt es als **8507 Renames** (Historie erhalten), dazu 11 neue Dateien + 1 Löschung. Wegen der Größe (8500+ Dateien, fremde Änderung) vorher Rückfrage gestellt → Justin gab grün. Secret-Scan sauber (einziger Treffer war ein PNG `01_token_vergleich.png`, kein echtes Secret). **Blockiert:** JustUpdate ist `behind=2` → NICHT gepusht, braucht erst `git pull --rebase` (kein Force). Übersprungen (sauber): itintechsolutionswebsite, JorllyJustinTransfer, nexus, PrivateBackup. Nebenbei in dieser Session: opencode-Setup repariert (OpenRouter Gratis-120b als Standardmodell + tok/s-Overlay-Plugin `token-meter.tsx`; TUI-Plugins gehören in `tui.json`, nicht `opencode.json`). -> [[itintech-firma]]

log 15.06.2026 19:15 Claude Code (Windows / Gaming-PC, JUSTINGAMINGPC) — **/push: 1 Repo gepusht (2 Commits).** [[itintech-firma]] (Itin-TechSolutions) → `cc43eda` refactor: JustLauncher-Host-URLs (app.py + README) von Port 5050 auf 8000 umgestellt, passt zum zentralen Port-Register [[PORTS]], per `JL_REMOTE_PORT` überschreibbar; `ddb9280` docs: neuer Konzept-Ordner **[[Solomon]].V0.01** mit Digitec-Screenshot ASUS Ascent GX10 (GB10-Superchip, 128 GB Unified Memory, CHF 3602) als Ausgangsmaterial für Recherche zu lokalen LLM-Geräten (DGX-Spark-Klasse). Push erst rejected (Remote war seit Scan weitergewandert, anderes Gerät) → sauber per `git pull --rebase` integriert, kein Force, dann gepusht (`906ced3`). **Bewusst NICHT committet:** `build_log.txt` (PyInstaller-Wegwerf-Artefakt im JustLauncher-Ordner) → sollte gitignored werden. Übersprungen (sauber, nichts zu tun): itintechsolutionswebsite, JorllyJustinTransfer, JustUpdate, PrivateBackup. -> [[itintech-firma]]

log 15.06.2026 16:11 Luna (Hermes / Mac mini) — JustTodo-Stand zur InputLuna-Verarbeitung live verifiziert: der Acino-Eintrag `0362f1b7` (`[Acino] Linie 9 – Tablettenlift 07.008: Notaus repariert`) ist in JustTodo tatsächlich auf `erledigt` gesetzt; zusätzlich geklärt, dass die Input-Unterordner `Offen`/`Abgeschlossen` nicht als separate JustTodo-Einträge übernommen wurden. Danach den `/log`-Workflow für nexus eingerichtet: neue Skill `nexus-chat-to-log` erstellt und Regel festgelegt, dass `log`/`/log` das Wichtigste aus dem heutigen Chat in das zentrale Wochenlog `00_Log/<Jahr>/KW_<KW>/LOG.md` schreibt. → [[justtodo]] [[00_Log/README]]

log 15.06.2026 15:40 Claude Code (Windows / Acino-Arbeitslaptop, ACINOLAPTOP) — **/push: 2 Repos gepusht.** [[justupdate]] → `3a289a3` fix: **v2.7.2 — leeres Live-Protokoll behoben (Regression aus v2.7.1).** Justin meldete „Live Protokoll immer leer". Ursache: die in v2.7.1 für die leserlichere Live-Ansicht neu eingeführte Funktion `Format-LiveLine` war nur **lokal in `Start-Maintenance`** definiert; der UI-Timer-Tick (DispatcherTimer) feuert aber erst, NACHDEM `Start-Maintenance` zurückgekehrt ist → die lokale Funktion existiert dann nicht mehr, der Aufruf wirft „nicht erkannt". Und weil die Zeile in der Render-Schleife **vor** dem Aufruf aus `$s.Lines` entfernt wird, verschluckte das `catch { break }` jede einzelne Zeile → Fortschrittsbalken lief, Live-Feld blieb leer. Logdatei war nie betroffen. **Per echtem Repro verifiziert** (DispatcherTimer-Tick nach Funktions-Rückkehr ⇒ exakt der „nicht erkannt"-Fehler; erster Repro-Versuch war falsch, weil ich den Dispatcher noch INNERHALB der Funktion pumpte — da lief der Scope noch). Fix: `function script:Format-LiveLine` (script:-Scope, im Timer-Tick erreichbar) + Version-Bump 2.7.1→2.7.2 (Header + Fallback) + CHANGELOG-Eintrag. Skript parst sauber. [[itintech-firma]] (Itin-TechSolutions) → `6431852` chore: **ClaudeSync auf neues Gerät ACINOLAPTOP angebunden** — `tree.md`-Sektion `## ACINOLAPTOP` ergänzt (der SessionStart-Hook hatte sie beim Start vermisst) + gerätelokale `local/ACINOLAPTOP.json` (PowerShell-SessionStart-Hook statt bash, bypassPermissions; vormals DESKTOP-AMISUVG, am 15.06. umbenannt, Windows speichert NetBIOS uppercase ⇒ %COMPUTERNAME%=ACINOLAPTOP). Keine Secrets in der JSON (reine Hook-/Permission-Config). Übersprungen (sauber, nichts zu tun): nexus war vor diesem Log-Eintrag clean. Kein `behind`, kein `--force`, nie `git add -A` im nexus (Log gezielt gestaged). → [[justupdate]] [[itintech-firma]] [[claudesync]]
